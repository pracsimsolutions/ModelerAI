---
id: embedfile
name: "embedfile"
kind: command
module: ""
signature: "embedfile(num mode, str projectpath, str filepath)"
aliases: []
tags: ["command"]
deprecated: false
replacedBy: null
description: "Embeds a file in a node"
seeAlso: []
source: "help\\CommandReference\\Commands.xml"
---

# embedfile

```flexscript
embedfile(num mode, str projectpath, str filepath)
```

Embeds a file in a node

This command is used to embed a file in a node. mode can take the following values, 1=embed, 2=extract, 3=remove.

