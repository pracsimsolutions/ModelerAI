---
id: removebundleentry
name: "removebundleentry"
kind: command
module: ""
signature: "removebundleentry(node datanode, int entrynum)"
aliases: []
tags: ["command", "group-statistics"]
deprecated: false
replacedBy: null
description: "Removes the entry entrynum (base 0) of the bundle"
seeAlso: []
source: "help\\CommandReference\\Commands.xml"
---

# removebundleentry

```flexscript
removebundleentry(node datanode, int entrynum)
```

Removes the entry entrynum (base 0) of the bundle

Removes the entry entrynum (base 0) of the bundle.

## Example

```flexscript
removebundleentry(x, 2);
```

