# FlexScript command catalog — full

Generated by tools/build-kb.js. 1216 commands total (790 active, 426 deprecated).
Deprecated entries are marked with ⚠ and a "use X" hint. The Deprecated section is at the bottom — prefer the active commands above it.

## (ungrouped)

| Command | Returns | Signature | Note |
|---|---|---|---|
| `avi` | — | `(int cmd, str filename, num tex, num frame)` | Not fully implemented |
| `cubemap` | — | `(int cmd, int FACEa, int FACEb, int FACEc, int FACEd, int FACEe, int FACEf)` | Attaches 2D images to vertices of a 3D model |
| `embedfile` | — | `(num mode, str projectpath, str filepath)` | Embeds a file in a node |
| `mediafile` | str | `(str filename)` |  |
| `mediapath` | str | `(str projectpath, str filepath, int mode)` |  |
| `simexec` | — | `(num cmd, num v1)` |  |
| `socketx` | — | `socketx(int serverclient, int cmd, int port=0, char* data="", linkedlist * thenode=SAFENODE)` | Not implemented yet |
| `switch_hidden` | num | `(obj object[, num off/on])` |  |
| `system_cld` | — | `()` |  |
| `system_clda` | — | `()` |  |
| `system_cli` | — | `()` |  |
| `system_clia` | — | `()` |  |
| `system_cln` | — | `()` |  |
| `system_clna` | — | `()` |  |
| `system_cls` | — | `()` |  |
| `system_cls` | — | `()` |  |
| `system_clsa` | — | `()` |  |
| `system_gld` | num | `()` |  |
| `system_glda` | num | `()` |  |
| `system_gli` | num | `()` |  |
| `system_glia` | num | `()` |  |
| `system_gln` | node | `()` |  |
| `system_glna` | node | `()` |  |
| `system_gls` | str | `()` |  |
| `system_glsa` | str | `()` |  |
| `system_ms` | — | `()` |  |
| `system_sld` | — | `()` |  |
| `system_slda` | — | `()` |  |
| `system_sli` | — | `()` |  |
| `system_slia` | — | `()` |  |
| `system_sln` | — | `()` |  |
| `system_slna` | — | `()` |  |
| `system_sls` | — | `()` |  |
| `system_slsa` | — | `()` |  |
| `trackpopup` | — | `(node thenode)` |  |
| `wmp` | — | `(num cmd, str file, HWND win, num left, num right, num top, num bottom, num controls)` | Opens the file in Windows Media Player |

## Communication

| Command | Returns | Signature | Note |
|---|---|---|---|
| `clientclose` | num | `(num socket)` | Closes the client socket |
| `clientconnect` | num | `(num socket, str hostname, num port)` | Attempts to connect the client socket |
| `clientcreate` | num | `()` | Creates a new client socket with TCP/IP protocol |
| `clientreceive` | — | `(num socket, str buffer, num buffersize, num noblock[1/0])` | Receives data sent to the socket |
| `clientsend` | num | `(num socket, str message[, num charlength])` | Sends a message across the socket |
| `excelclose` | — | `(num save [1/0])` | Close the currently open Excel workbook |
| `excelcreatesheet` | — | `(str name)` | Creates a new worksheet in the currently open Excel workbook |
| `excelcreateworkbook` | num | `()` | Create an Excel workbook |
| `exceldeleterange` | — | `(str rangename)` | Deletes the specified range of the currently open Excel workbook |
| `exceldeletesheet` | — | `(str name [, int allowwarning])` | Delete the worksheet in the currently open Excel workbook |
| `excelexporttable` | — | `(str/node/num table, num startrow, num startcol, num numrows, num numcols [, num rowchar, num colchar])` | Exports data from a table within FlexSim to the currently open Excel workbook |
| `excelgetrangeparam` | num | `(str rangename, num param)` | Retrieves a location parameter about the named range of the currently open Excel workbook |
| `excelgetrangesheet` | str | `(str rangename)` | Gets the name of the worksheet associated with rangename of the currently open Excel workbook |
| `excelimporttable` | — | `(str/node/num table, num startrow, num startcol, num numrows, num numcols)` | Imports data from the currently open Excel workbook to a table in FlexSim |
| `excellaunch` | — | `([str launchdir])` | Launch Microsoft Excel |
| `excelmultitableexport` | — | `()` | Starts the automatic exporting of data from FlexSim tables to Excel |
| `excelmultitableimport` | — | `()` | Starts the automatic importing of data from Excel to FlexSim tables |
| `excelopen` | num | `(str workbook)` | Open the Excel workbook |
| `excelquit` | — | `()` | Close Microsoft Excel |
| `excelrangereadnum` | num | `(str rangename, num row, num column)` | Read the numeric value from a cell of the specified range of the currently open Excel workbook |
| `excelrangereadstr` | str | `(str rangename, num row, num column [, num rowchar, num colchar])` | Read the text value from a cell of the specified range of the currently open Excel workbook |
| `excelrangewritenum` | — | `(str rangename, num row, num column, num value)` | Write the numeric value to a cell of the specified range of the currently open Excel workbook |
| `excelrangewritestr` | — | `(str rangename, num row, num column, str value)` | Write the string value to a cell of the specified range of the currently open Excel workbook |
| `excelreadcell` | var | `(num row, num col[, num flags])` | Returns the value of a cell in the active worksheet as a variant |
| `excelreadnum` | num | `(num row, num column [, num rowchar, num colchar])` | Read the numeric value from a cell of the active sheet of the currently open Excel workbook |
| `excelreadstr` | str | `(num row, num column [, num rowchar, num colchar])` | Read the text value from a cell of the active sheet of the currently open Excel workbook |
| `excelsave` | — | `([str filename])` | Saves the currently open Excel file |
| `excelsetrange` | — | `(str rangename, num startrow, num startcolumn, num endrow, num endcolumn)` | Creates or changes a named range of the currently open Excel workbook |
| `excelsetsheet` | num | `(str sheetname)` | Sets the active worksheet of the currently open Excel workbook |
| `excelversion` | str | `()` | Gets the version of Microsoft Excel |
| `excelwritenum` | — | `(num row, num column, num value [, num rowchar, num colchar])` | Write the numeric value to a cell of the active sheet of the currently open Excel workbook |
| `excelwritestr` | — | `(num row, num column, str value [, num rowchar, num colchar])` | Write the string value to a cell of the active sheet of the currently open Excel workbook |
| `requestdecision` | — | `(str/node/num object)` |  |
| `rundocument` | num | `(str command, str path+filename)` | Open a file using the default application for the file type |
| `runprogram` | num | `(str commandline)` | Run an external program |
| `serveraccept` | num | `(int noblocking)` | Gets the server will attempt to accept a client connection |
| `servercloseconnection` | num | `(int connection)` | Closes the client connection |
| `serverclosemain` | num | `()` | Closes the main server socket and all open connections |
| `servercreatemain` | num | `(int port)` | Creates a server socket that is listening for connections to the specified port |
| `serverreceive` | — | `(int connection, char *buffer, int bufsize, int noblocking)` | Receives a message from the connection |
| `serversend` | num | `(int connection, char* message [, num charlength] )` | Sends a message to the client connect to connection |
| `socketend` | num | `()` | Shuts down the processes started with socketinit() |
| `socketinit` | num | `()` | Initializes background processes that allow Windows Sockets to be used |

## Conversions

| Command | Returns | Signature | Note |
|---|---|---|---|
| `apchar` | str | `(str string)` | Asserts string as a char* |
| `cm` | num | `(num centimeters)` | Gets the number of meters in the number of centimeters |
| `convert` | var | `(var value, var from, var to)` | Converts the given value (a time) from one time format to another |
| `days` | num | `(num days)` | Gets the number of model time units in the give number of days |
| `fpm` | num | `(num feetperminute)` | Gets the number of model length/time units in the number of feet/minute |
| `fps` | num | `(num feetpersecond)` | Gets the number of model length/time units in the number of feet/minute |
| `ft` | num | `(num feet)` | Gets the number of model length units in the number of feet |
| `hours` | num | `(num hours)` | Gets the number of model time units in the number of hours |
| `in` | num | `(num inches)` | Gets the number of model length units in the number of inches |
| `minutes` | num | `(num minutes)` | Gets the number of model time units in the number of minutes |
| `mph` | num | `(num milesperhour)` | Gets the number of model length/time units in the number of miles/hour |
| `seconds` | num | `(num seconds)` | Gets the number of model time units in the number of seconds |
| `tonode` | node | `(thing)` | Assert type as a node pointer |
| `tonum` | num | `(thing)` | Assert type as a number |
| `toptr` | — | `(thing)` |  |

## Development

| Command | Returns | Signature | Note |
|---|---|---|---|
| `addcommand` | — | `(obj codenode, str commandname, str description, str parameters, str example [, str cppprefix, str shortdescription])` |  |
| `backupfile` | — | `(str filepath)` |  |
| `buildall` | — | `()` |  |
| `buildnodeflexscript` | — | `(node codenode)` |  |
| `changedisplay` | — | `(num width, num height, num depth, num fullscreen, num test)` | Change resolution |
| `cleannodeflexscript` | — | `(node startnode)` |  |
| `cmdcompile` | — | `()` |  |
| `cmddelete` | — | `(node thenode)` |  |
| `cmdexit` | — | `()` |  |
| `cmdloadbyextension` | — | `(str filename, num silent)` |  |
| `cmdloadsession` | — | `(str filename)` |  |
| `cmdloadtree` | — | `(obj desttree, str filename [, num nobuild])` |  |
| `cmdnew` | — | `()` |  |
| `cmdopen` | — | `()` |  |
| `cmdsaveas` | — | `()` |  |
| `cmdsavesession` | — | `(str filename)` |  |
| `cmdsavetree` | — | `(obj tree, str filename)` | Save a Tree by name |
| `consoleexecutefs` | — | `(HWND input, HWND output)` |  |
| `copyattributes` | — | `(obj destination, obj source, num mask)` |  |
| `copycolor` | — | `(obj highlightedobj)` |  |
| `copylabel` | — | `(obj highlightedobject, str/obj labelnamenode)` |  |
| `copylabels` | — | `(obj highlightedobj[, num mode])` |  |
| `copyvariable` | — | `(obj fromobject, obj variablenamenode)` |  |
| `copyvariables` | — | `(obj fromobject)` |  |
| `copyvisuals` | — | `(obj highlightedobj [, num copyshapefactorsalso])` |  |
| `daemonstate` | num | `(num thing)` |  |
| `documentusercommands` | — | `()` |  |
| `excelupdatepreferences` | — | `()` | SYSTEM USE ONLY |
| `fgldebuglevel` | — | `(num level [0-3])` |  |
| `fileisreadonly` | num | `(str filename)` |  |
| `fillwithatts` | — | `(obj object, num attgroup)` |  |
| `fscheckall` | — | `(node startingnode)` |  |
| `function_n` | — | `(obj object, num index [, num param_a,...])` |  |
| `function_s` | — | `(obj object, str name [, num param_a,...])` |  |
| `getapplicationtitle` | str | `()` |  |
| `getnextparametersobject` | obj | `(obj startobject, str guifocus, num doguifocusclass, num searchforward)` |  |
| `getproperty` | — | `(str filename, num property)` |  |
| `gettreedrawposition` | num | `(node destnode, node roottree)` |  |
| `groupconnectfrom` | — | `(obj object, str character)` |  |
| `groupconnectto` | — | `(obj object, str character)` |  |
| `groupcopyconnections` | — | `(obj object)` |  |
| `help` | — | `()` |  |
| `howmanyattributes` | num | `()` |  |
| `howmanycommands` | num | `()` |  |
| `license` | num | `()` | Gets the current license type |
| `licenseactivation` | num | `(num operation[, str buffer, var p1, node callback])` |  |
| `maintenance` | — | `(num command[, num state, str strparam])` |  |
| `modeleditmode` | — | `(num/str mode)` |  |
| `postclosewindowmessage` | — | `(obj view)` | Posts a Windows Message to close the window |
| `postmovewindowupdate` | — | `(obj view)` |  |
| `postwindowmessage` | — | `(HWND window, num msg[, num/str/node wParam, num/str/node lParam])` |  |
| `rebindobjectattributes` | — | `(node thenode)` | Rebinds object attributes |
| `recover` | — | `(num phase)` | Load recovery files |
| `refreshcommandlist` | — | `()` |  |
| `refreshglobalvariables` | — | `()` |  |
| `refreshmodelcommands` | — | `()` |  |
| `sendwindowmessage` | — | `(HWND window, num msg[, num/str/node wParam, num/str/node lParam])` |  |
| `setapplicationtitle` | — | `(str title)` |  |
| `setmessageloopsleep` | — | `(num interval, num sleeptime)` |  |
| `setproperty` | — | `(str filename, num property, str newvalue)` |  |
| `setticker` | — | `(node thenode[, num tickspersecond])` |  |
| `statsenablemask` | — | `(num mask)` |  |
| `stick` | var | `(num stickno, num command [, HWND/num win, var/num p])` |  |
| `suspenddraw` | num | `(num bitmask)` |  |
| `switch_variable` | num | `(obj object, str var[, num off/on])` |  |
| `toggleallcodestates` | — | `(node startpoint, num ascpp)` | Toggles all code nodes under startpoint as either C++ or flexscript |
| `tracemaskevents` | — | `(num mask)` |  |
| `tracemaskobject` | — | `(obj object)` |  |
| `tracemasktime` | — | `(num start, num end)` |  |
| `uselisteners` | — | `(num mask)` |  |

## Drawing

| Command | Returns | Signature | Note |
|---|---|---|---|
| `bindtexture` | — | `(num textureid)` |  |
| `draw3dtext` | — | `(str text[, num asBitmap])` | Draws text in a 3d view |
| `drawbox` | — | `(num x, num y, num z, num sx, num sy, num sz, num glbegin)` | Draws a simple box |
| `drawcolumn` | — | `(num x, num y, num z, num totalsides, num baseradius, num topradius, num height, num rx, num ry, num rz, num red {0-255}, num green {0-255}, num blue {0-255} [, num opacity {0-1}, num textureindex, num textureXrepeat, num textureYrepeat])` | Draws a 3D column |
| `drawcube` | — | `(num x, num y, num z, num sx, num sy, num sz, num rx, num ry, num rz, num red {0-255}, num green {0-255}, num blue {0-255} [, num opacity {0-1}, num textureindex, num textureXrepeat, num textureYrepeat])` | Draws a 3D cube |
| `drawcylinder` | — | `(num x, num y, num z, num baseradius, num topradius, num height, num rx, num ry, num rz, num red {0-255}, num green {0-255}, num blue {0-255} [, num opacity {0-1}, num textureindex])` | Draws a 3D clyinder |
| `drawdisk` | — | `(num x, num y, num z, num innerradius, num outerradius, num startangle, num sweepangle, num rx, num ry, num rz, num red {0-255}, num green {0-255}, num blue {0-255} [, num opacity {0-1}, num textureindex])` | Draw a 3D disk |
| `drawflattext` | — | `(str text)` |  |
| `drawfont` | — | `(num font)` | Specifies the font to be used with drawtext() |
| `drawimage` | — | `(obj view, str imagepath, num x, num y, num width, num height, num scaling)` | Draws an image |
| `drawline` | — | `(obj view, num x1, num y1, num z1, num x2, num y2, num z2, num red {0-1}, num green {0-1}, num blue {0-1})` | Draws a planar line |
| `drawobject` | — | `(obj view, num shapeindex, num textureindex[, num animation, treenode shapedata])` | Draws a 3D shape |
| `drawpie` | — | `(obj view, num x, num y, num z, num x2, num y2, num z2, num startangle, num endangle, num red {0-1}, num green {0-1}, num blue {0-1})` | Draws a flat pie shape |
| `drawquad` | — | `(obj view, num x1, num y1, num z1, num x2, num y2, num z2, num x3, num y3, num z3, num x4, num y4, num z4, num red {0-1}, num green {0-1}, num blue {0-1})` | Draws a planar quadrilateral |
| `drawrect` | — | `(obj view, num x1, num y1, num z1, num x2, num y2, num z2, num red {0-1}, num green {0-1}, num blue {0-1})` | Draws a planar rectangle |
| `drawrectangle` | — | `(num x, num y, num z, num length, num width, num rx, num ry, num rz, num red {0-255}, num green {0-255}, num blue {0-255} [, num opacity {0-1}, num textureindex, num textureXrepeat, num textureYrepeat])` | Draw a 3D rectangle |
| `drawshadow` | — | `(num phase (1,2,3))` | In OnDraw: draw shadow volume |
| `drawsphere` | — | `(num x, num y, num z, num radius, num red {0-255}, num green {0-255}, num blue {0-255} [, num opacity {0-1}, num textureindex])` | Draw a 3D sphere |
| `drawtext` | — | `(obj view, str text, num x, num y, num z [, num width, num height, num thickness, num rx, num ry, num rz, num red {0-1}, num green {0-1}, num blue {0-1} , num opacity {0-1}, num flags])` | Draws text |
| `drawtomodelscale` | — | `(obj object)` | Gets the current drawing scale of the model |
| `drawtoobjectscale` | — | `(obj object)` | Scale subsequent draw commands to the size of the object |
| `drawtriangle` | — | `(obj view, num x1, num y1, num z1, num x2, num y2, num z2, num x3, num y3, num z3, num red {0-1}, num green {0-1}, num blue {0-1})` | Draws a planar triangle |
| `fglColor` | — | `(num red {0-1}, num green {0-1}, num blue {0-1}[, num alpha {0-1}])` | Sets the color for drawing a mesh in OpenGL |
| `fglDisable` | — | `(num setting)` | Disables an OpenGL draw setting |
| `fglEnable` | — | `(num setting)` | Enables an OpenGL draw setting |
| `fglinfo` | num | `(num operation, obj view)` | Gets information about a 3D view's draw state |
| `fglLoadIdentity` | — | `()` |  |
| `fglMatrixMode` | — | `(num mode)` | Specifies which matrix stack is the target for subsequent matrix operations |
| `fglNormal` | — | `(num x, num y, num z)` | Sets the normal vector for shading subsequent vertices in OpenGL |
| `fglPopMatrix` | — | `()` | Pops the current OpenGL drawing context from the matrix stack |
| `fglPushMatrix` | — | `()` | Pushes the current OpenGL drawing context onto the matrix stack |
| `fglRotate` | — | `(num degrees, num x, num y, num z)` | Rotates the current OpenGL drawing context by degrees around axis x,y,z |
| `fglScale` | — | `(num x, num y, num z)` | Scales the current OpenGL drawing context by x,y,z |
| `fglTranslate` | — | `(num x, num y, num z)` | Translates the current OpenGL drawing context by x,y,z |
| `getdrawtextmetric` | num | `(str text, num fontsize, num metric[, num a])` |  |
| `glIsEnabled` | — | `(num setting)` | Returns 1 if the OpenGL setting is enabled, 0 otherwise |
| `glLineWidth` | — | `(num linewidth)` | Specifies the line width for subsequent lines draw in OpenGL |
| `glPointSize` | — | `(num pointsize)` | Specifies the point size in pixels for subsequent vertices drawn in OpenGL |
| `mesh` | — | `(node meshnode, int pervertexattribs, int flags)` | Creates an empty mesh and assigns it to meshnode |
| `meshaddindex` | num | `(node meshnode, int index)` | Adds an indexed vertex to a mesh |
| `meshaddvertex` | num | `(node meshnode)` | Adds a vertex to a mesh |
| `meshdraw` | — | `(node meshnode, int drawmode, int offset, int count [, int stride])` | Draws a mesh |
| `meshinfo` | num | `(node meshnode, int type[,int index, int param])` | Gets the specified information from a mesh |
| `meshsetattrib` | — | `(node meshnode, int attribid, double param1 [, double param2, double param3, double param4])` | Adds/sets the value of attribid to the vertindex of a mesh |
| `meshsetvertexattrib` | — | `(node meshnode, int vertindex, int attribid, double param1 [, double param2, double param3, double param4])` | Sets the value of attribid at the vertindex of a mesh |
| `setdrawnamefunction` | — | `(num boxwidth, num boxheight, node nodefunction[, num pval1, num pval2, num pval3])` |  |
| `spacebase` | — | `([num view/object])` |  |
| `spacerotate` | — | `(num x, num y, num z)` |  |
| `spacescale` | — | `(num x, num y, num z)` |  |
| `spacetranslate` | — | `(num x, num y, num z)` |  |
| `textureaxis_s` | — | `(num x, num y, num z, num a)` |  |
| `textureaxis_t` | — | `(num x, num y, num z, num a)` |  |

## Events

| Command | Returns | Signature | Note |
|---|---|---|---|
| `addtoeventtypelegend` | — | `(str name, num parameter, num value, str legendname)` |  |
| `createevent` | num | `(obj object, num time, num code[, str data, node involved])` |  |
| `createtickevent` | — | `(obj fluidobject)` | Creates a tick event for fluidobject and all objects assigned to that Ticker |
| `delayevents` | — | `(num value)` |  |
| `delayeventsofobject` | — | `(obj object, num value)` |  |
| `destroyevent` | num | `(node event)` |  |
| `destroyeventsofobject` | — | `(obj object [, num simtime, num code, str data, node involved])` | Destroys events associated with object |
| `destroymessageevents` | — | `(obj object, num time, obj fromobj[, num param1, num param2, num param3])` | Destroys message events associated with object |
| `eventcode` | num | `<no parentheses>` |  |
| `eventdata` | str/num/node | `<no parentheses>` |  |
| `eventget` | num/node/str | `(num index, num thing)` | An event attribute for the event at index |
| `eventloginfo` | str | `(num operation, num n1)` |  |
| `eventqty` | num | `()` | Gets the number of events currently pending |
| `listenerinfo` | num/node | `(int info)` |  |
| `logevent` | — | `(num/str index/name[, obj object, obj involved, num p1, num p2, num p3, num p4])` |  |
| `registereventtype` | num | `(str name)` |  |
| `startlisten` | — | `(obj listener)` |  |
| `stoplisten` | — | `(obj listener)` |  |

## FixedResources

| Command | Returns | Signature | Note |
|---|---|---|---|
| `evaluatepullcriteria` | num | `(obj fixedresource, obj item, num portnr[, num bypassflags])` | Causes the FixedResource object to re-fire its pull requirement |
| `getiteminvolved` | node | `(obj item)` | Gets the object reference stored in the FixedResource involved variable for the item |
| `getitemsendto` | num | `(obj item)` | Gets the value of the evaluated sendto for the item |
| `getitemstate` | num | `(obj item)` | Gets the FixedResource state of the flowitem |
| `getitemvar` | num | `(obj item, num var)` | Gets the value of a FixedResource variable for the item |
| `holditem` | — | `(obj item)` | Holds the item if it has already been released |
| `inititem` | — | `(obj item)` | Initializes the FixedResource variables of a flowitem |
| `pullitem` | num | `(obj fixedresource, obj item, num portnr[, num bypassflags])` |  |
| `receiveitem` | — | `(obj station [, num port ] )` | Tells the FixedResource to open inputs and receive a flowitem |
| `recycleitem` | — | `(obj item , num binrank )` | Recycles the flowitem to the flowitem bin |
| `releaseitem` | num | `(obj flowitem [, num port ])` | Releases the flowitem from its current object |
| `resetuniqueid` | — | `(obj flowitem)` | Resets the uniqueid attributes on a flowitem to a value that has not been assigned to another flowitem |
| `resumetransportsin` | — | `(obj fixedresource [, num saverank])` |  |
| `resumetransportsout` | — | `(obj fixedresource [, num saverank])` |  |
| `savestoppedtransportin` | — | `(obj fixedresource, obj taskexecuter)` |  |
| `savestoppedtransportout` | — | `(obj fixedresource, obj taskexecuter)` |  |
| `setiteminvolved` | — | `(obj item, obj involved)` |  |
| `setitemsendto` | — | `(obj item, num value)` |  |
| `setitemvar` | — | `(obj item, num var, num value)` |  |
| `transportincomplete` | — | `(obj object, obj item [, num inport, obj transporter])` | Notifies the FixedResource object that item will now be moved into it |
| `transportoutcomplete` | — | `(obj object, obj item [, num outport, obj transporter])` | Notifies the FixedResource object that item will now be moved out of it |

## Kinematics and Splines

| Command | Returns | Signature | Note |
|---|---|---|---|
| `addkinematic` | num | `(node infonode, num x, num y, num z, num targetspeed, num acc, num dec, num startspeed, num endspeed, num starttime, num type )` | Adds one kinematic operation to a set of kinematics |
| `deactivatekinematics` | — | `(node kinematicsnode)` | Causes kinematics to do nothing when updatekinematics() is called |
| `dynamicsplineall` | — | `(node starttree)` |  |
| `fastspline` | — | `(obj spline, num perc [0-1])` |  |
| `fastsplineall` | — | `(obj starttree {model}, num res)` |  |
| `fastsplineupdate` | — | `(obj spline, num res)` |  |
| `getkinematics` | num | `(node infonode, num querytype[, num kinematicindex, num updatetime/traveldist])` | Gets information from the specified kinematics operation |
| `initkinematics` | — | `(node infonode, obj/num object/x[, num flags/y, num z, num rx, num ry, num rz, num flags, num unused])` | Initializes a kinematic operation |
| `profilekinematics` | — | `(node infonode, num kinematicindex)` | Prints information about the specified kinematics operation to the output console |
| `scalekinematicstotime` | num | `(node kinematics, int kinematicnr, double scaletotime)` | Scales the kinematics to a defined time |
| `setkinematicsrotoffset` | — | `(node infonode, num rx, num ry, num rz)` |  |
| `spline` | — | `(obj spline, num perc)` |  |
| `splinedraw` | — | `(obj object)` |  |
| `splinedrawextrusion` | — | `(obj object, num step, num wide, num high, num top, num side)` |  |
| `splinelength` | num | `()` |  |
| `splineposobject` | — | `(obj object)` |  |
| `splinerx` | num | `()` |  |
| `splinery` | num | `()` |  |
| `splinerz` | num | `()` |  |
| `splinex` | num | `()` |  |
| `spliney` | num | `()` |  |
| `splinez` | num | `()` |  |
| `updatekinematics` | — | `(node infonode, obj object [, num updatetime])` | Updates the location of the object based on its kinematics |

## Language

| Command | Returns | Signature | Note |
|---|---|---|---|
| `a` | node | `<no parentheses>` |  |
| `break` | — | `()` |  |
| `case` | — | `value:` |  |
| `continue` | — | `()` |  |
| `default` | — | `:` |  |
| `else` | — | `{ }` |  |
| `for` | — | `(initalstatement; whiletrueexpression ; countstatement)` |  |
| `forobjecttreeunder` | — | `(node container) { }` |  |
| `if` | — | `(num condition) { }` |  |
| `return` | — | `(num value)` |  |
| `stringcompare` | num | `(str text1, str text2)` | Compare strings for alphabetical order |
| `switch` | — | `(num value)` |  |
| `while` | — | `(num expression) {}` |  |

## Media

| Command | Returns | Signature | Note |
|---|---|---|---|
| `applyshapefactors` | num | `(obj object)` | Applies all of the shape factors to object |
| `autoloadallmedia` | — | `([node thenode])` |  |
| `autoloadimages` | — | `()` |  |
| `autoloadshapes` | — | `()` |  |
| `autoloadsounds` | — | `()` |  |
| `flyactive` | num | `()` | Returns 1 if there is an fly path running, 0 otherwise |
| `flyendnode` | node | `(node x)` | Gets a reference to the node that represents the stopping point for the current fly path |
| `flypath` | — | `(obj view, num pathno [, num singlerun])` |  |
| `flypathadd` | — | `(obj view, num pathno)` |  |
| `flypathreset` | — | `(obj view, num pathno)` |  |
| `flystart` | — | `(obj view)` |  |
| `flystop` | — | `()` |  |
| `flyto` | — | `(obj view, num endtime, num x, num y, num z, num rx, num ry, num rz)` |  |
| `flyupdate` | — | `()` | Updates the currently active fly path |
| `getshapeindex` | num | `(str name)` | Gets the index of a loaded 3D shape |
| `getsoundindex` | num | `(str name)` | Gets the index of a loaded sound |
| `gettextureindex` | num | `(str name)` | Gets the index of a loaded image |
| `loadimage` | num | `(str file, str name)` |  |
| `loadthreedobject` | num | `(str file, str name)` |  |
| `mediareset` | — | `()` |  |
| `objectsoundcode` | num | `(obj object)` | Gets the index of object's default sound |
| `shapeinfo` | var | `(num info[, p1, p2, p3])` | Gets or sets information about loaded 3D shapes. |
| `soundchange` | — | `(obj view, num sound, num volume, num tempo, num repeat, num balance)` | Change a currently playing sond |
| `soundload` | — | `(str path+filename)` | Load a sound into FlexSim |
| `soundplay` | — | `(obj view, num soundindex, num volume [, num tempo, num repeat y/n, num balance])` | Play a loaded sound |
| `soundplayfile` | — | `(obj view, str path+filename, num volume [, num tempo, num repeat, num balance])` | Play a sound directly from a file |
| `soundstop` | — | `(obj view, num sound)` | Stop a sound playing |
| `unloadimage` | num | `(int textureid)` |  |
| `viewtofile` | — | `(obj view, str path[, num width, num height])` | Saves the specified view as a bitmap image |

## Miscellaneous

| Command | Returns | Signature | Note |
|---|---|---|---|
| `addsystemcontrolleritem` | — | `(obj systemcontroller, num productid, num linenum, num target, num starttime [, num newcolumn1, num newcolumn2, num newcolumn3, num newcolumn4])` | For Dynamo use |
| `commandlineparam` | str | `(str param)` |  |
| `contextdragconnection` | — | `(obj fromobject, obj toobject, str/num characterpressed)` | Creates an A or S connection between objects |
| `createuserlibrary` | — | `()` |  |
| `deleteselected` | — | `()` |  |
| `dropuserlibraryobject` | node | `(obj object [, obj ontoobject, num x, num y, num z, obj ontoview, num viewalreadyset])` |  |
| `duplicateselected` | — | `()` |  |
| `executefsfile` | — | `(str filename, num direct)` |  |
| `filebrowse` | str | `(str extension, str description, str startpath)` | Opens a file selection dialog |
| `firecreateevent` | — | `(obj object, num x, num y, num z, num iscopy [, num libraryfunctiononly])` |  |
| `flipselected` | — | `(num firstX, num firstY, num secondX, num secondY[, node roottree])` | Flip selected objects across an axis |
| `geteventtime` | num | `(str EventName, num mode[1/2/3])` | Gets a UserEvent's event time |
| `getlastfiletype` | num | `()` |  |
| `getlinecontrollerdata` | num | `(obj linecontroller, num datatype, num column)` | For Dynamo use |
| `getmodelunit` | num/str/node | `(num querytype)` |  |
| `getreportnum` | num | `(node report, int column, int rowsback)` | For Dynamo use |
| `groupsetnames` | — | `(node namenode, num startnum, num append)` |  |
| `hideprogressbar` | — | `()` |  |
| `highestconnection` | num | `()` | Gets the highest numbered server connection index |
| `insertcopy` | — | `(obj originalobject, obj containerobject)` | Creates a copy of the flowitem and moves it into the container object |
| `integrityscan` | — | `(obj class, obj start)` |  |
| `isprojectcompiled` | num | `()` |  |
| `loadselectedfromfile` | — | `(obj highlightedobj)` |  |
| `loadstate` | — | `()` |  |
| `loaduserlibrary` | — | `(str libfile [, obj libraryview, num reloadmedia, num changeitemcurrent, num autoinstall])` |  |
| `memberadd` | — | `(obj childobject, str parentname, num type)` |  |
| `memberremove` | — | `(obj childobject, str parentname, num type)` |  |
| `memberremoveall` | — | `(str parentname, num type)` |  |
| `moveobject` | — | `(obj object, obj location[, num outport])` | Moves object to the specified location |
| `moveselectedintohighlighted` | — | `(obj highlightedobj [, node selectedcontainer])` |  |
| `notifylinecontroller` | — | `(node object, int controllernum, num output, num sendinglast [0/1])` | For Dynamo use |
| `pickoptiononselect` | — | `(obj view)` |  |
| `rotateselected` | — | `(double angle, double x, double y[, obj roottree])` |  |
| `savebyname` | — | `(obj tree, str filename)` |  |
| `savemodelprepare` | — | `()` |  |
| `saveselectedtofile` | — | `()` |  |
| `savestate` | — | `()` |  |
| `savetree` | — | `(node tree, str extension, str description, str startingpath)` |  |
| `searchfornode` | — | `(str/node searchstr, node startnode, num findnr[, num searchobjects])` |  |
| `seteventtime` | — | `(str EventName, num time, num timemode[1/2], num createevent[1/0])` | Sets the "Repeat Event Time" for the global UserEvent |
| `setprogressbar` | — | `(num progress)` |  |
| `showprogressbar` | — | `(str caption)` |  |
| `so` | node | `()` | Gets a reference to the last node designated as so() |
| `startuploadlibraries` | num | `([num viewalreadyset])` |  |
| `transfernodeobj` | — | `(node movenode, node container)` | Moves movenode into the container node's object data |
| `updatereport` | — | `(num/str/node report)` | For Dynamo use |
| `userinput` | var | `(obj targetnode, str text [, int stop])` | Prompts the user for a value in a dialog box |
| `validatename` | — | `(obj node, num isviewtext [, obj object])` |  |

## Model Execution

| Command | Returns | Signature | Note |
|---|---|---|---|
| `endreplication` | — | `(num onlyifexperimenting)` | Ends the current experimenter replication |
| `fastforward` | — | `()` |  |
| `getrunstate` | num | `([int checkProcessingEvent])` | Returns 1 if the model is currently running, 0 if not |
| `go` | — | `([num fast])` | Starts the simulation run |
| `realtime` | num/str | `([num timecode = 0])` | Gets the real clock time |
| `resetexperiment` | — | `()` |  |
| `resetlayer` | — | `(obj layer)` |  |
| `resetmodel` | — | `([num buildflexscript])` | Resets the model |
| `resetstats` | — | `()` |  |
| `runfunction` | — | `(num noreset)` |  |
| `runspeed` | num | `(num speed[, num precisionMode])` | Sets the simulation speed |
| `skip` | — | `()` |  |
| `step` | — | `()` |  |
| `stop` | — | `([num fast])` | Stops the simulation run |
| `stoptime` | num | `(num time [, num stopNum=1])` | Sets the time when the simulation should stop |
| `updatestates` | — | `()` | Updates the states of all objects in the model |

## NetworkNodes

| Command | Returns | Signature | Note |
|---|---|---|---|
| `closenodeedge` | — | `(obj object, num edgenum)` | Closes entry to the networknode's edge |
| `getedgedist` | num | `(obj netnode, num edgenum)` | Gets the distance of the edge of a network node |
| `getedgespeedlimit` | num | `(obj netnode, num edgenum)` | Gets the speed limit of the edge of a network node |
| `getnetnode` | obj | `(obj object, num nodenr)` | Gets the nth network node connected to object with an A connection line |
| `getnetnodespline` | obj | `(obj netnode, num edgenr)` | Gets a reference to the spline head of netnode's edge edgenr |
| `getnetworkdistance` | num | `(num fromnetnoderank, num tonetnoderank)` | Gets the network distance between two network nodes |
| `getnextnetnode` | num | `(num fromnn, num tonn)` | Gets the rank of the next network node from fromnn in order to get to tonn |
| `getnextnetnodeoutport` | num | `(num fromnn, num tonn)` | Gets the output port of the network node fromnn in order to get to tonn |
| `gettenavigator` | obj | `(obj taskexecuter)` | Gets the current navigator that a taskexecuter is connect to |
| `gettenetnode` | obj | `(obj taskexecuter)` | Gets the current network node that a taskexecuter is attached to with an A Connection |
| `gettenetworkinfo` | num | `(obj taskexecuter, obj destobj, num info)` | Gets information regarding the current state of a taskexecuter on a node network |
| `isedgeforward` | num | `(obj netnode, num edgenr)` | Returns 1 if the network node's edge is forward, 0 otherwise |
| `netnodefromrank` | obj | `(num rank)` | Returns the network node member at rank rank |
| `opennodeedge` | — | `(obj networknode, num edgenum)` | Opens entry to the networknode's edge |
| `optimizenetwork` | — | `()` | Causes the model's network navigator to regenerate its distance table |
| `rankfromnetnode` | num | `(obj netnode)` | Returns the member rank of network node netnode |
| `reassignnetnode` | — | `(obj taskexecuter, obj networknode)` |  |
| `redirectnetworktraveler` | — | `(obj traveler, obj newdest)` |  |
| `trafficcontrolinfo` | num/node | `(obj trafficcontrol, num info [, num a, num b])` | Gets information on a traffic controller |

## Object Data

| Command | Returns | Signature | Note |
|---|---|---|---|
| `assertvariable` | node | `(obj object, str variablename, num datatype)` | Gets a reference to variable variablename of object, creates the variable if it does not exist |
| `classobject` | obj | `(obj object)` | Returns the class object of an instance |
| `contentunder` | num | `(node thenode [, num objectsonly)` |  |
| `emptyfluidobject` | — | `(obj object)` | Sets the contents of the fluid object to 0 |
| `fluidcontent` | — | `(obj fluidobject)` | Gets the current fluid content of fluidobject |
| `fluidmaxcontent` | — | `(obj fluidobject)` | Gets the max fluid content of fluidobject |
| `getanimationvar` | obj/num | `(obj object, num/str animation, str varName)` | Gets the current value of an animation variable |
| `getcreationtime` | num | `(obj object)` | Gets the time that a flow item was created |
| `getentrytime` | num | `(obj object)` | Gets the time that a flow item entered the current object it is in |
| `getframe` | num | `(obj object)` | Gets the current Shape Frame being drawn for object |
| `getobjectshapeindex` | num | `(obj object)` | Gets the index number of the 3D shape assigned to object |
| `getobjecttextureindex` | num | `(obj object)` | Gets the index number of the image assigned as the texture for the object's 3D shape |
| `getvarnode` | node | `(obj object, str/num var)` | Gets a node reference to an object's variable |
| `getvarnum` | num | `(obj object, str/num var)` | Gets the value of an object's numeric variable |
| `getvarstr` | str | `(obj object, str/num var)` | Gets the value of an object's string variable |
| `isclasstype` | num | `(obj object, num/str classtype)` | Compares the object to classtype, returns 1 for yes, 0 for no |
| `reassignticker` | — | `(obj fluidobject, obj ticker[, int resortticker])` | Reassigns the Fluid Object's ticker |
| `setanimationvar` | — | `(obj object, num/str animation, str varName, num/obj toValue)` | Sets the value of an animation variable on an object |
| `setframe` | — | `(obj object, num/str toframe)` | Sets the shape frame of an object |
| `setobjectbillboard` | — | `(obj object, num mode)` | Sets the billboard attribute of the visual tool object |
| `setobjectshapeindex` | — | `(obj object, num index)` | Sets the index number for the 3D shape of an object |
| `setobjecttextureindex` | — | `(obj object, num index)` | Sets the index number of the image assigned as a texture for the object's 3D shape |
| `setresetposition` | — | `(obj object)` | Sets the reset position of the object |
| `setvarnum` | — | `(obj object, str/num var, num value)` | Sets the value of an object's numeric variable |
| `setvarstr` | — | `(obj object, str/num var, str value)` | Sets the value of an object's string variable |
| `switch_enablestats` | num | `(obj object[, num off/on])` | Get or set the "stats" flag of the object |
| `switch_hideconnectors` | num | `(obj object[, num off/on])` | Get or set the "Hide Connectors" flag of the object |
| `switch_hidecontents` | num | `(obj object[, num off/on])` | Get or set the "Show Contents" flag of the object |
| `switch_hidelabel` | num | `(obj object[, num off/on])` | Get or set the "Show Name" flag of the object |
| `switch_hideshape` | num | `(obj object[, num off/on])` | Get or set the "Show 3D Shape" flag of the object |
| `switch_showobject` | num | `(obj object[, num off/on])` | Expand/collaps the object data of an object node |
| `updatelocations` | — | `(obj object)` | Updates the location of the object and the objects inside of it |
| `updatestate` | — | `(obj object [, num profilenr])` | Updates the state of the object |

## Object Functions

| Command | Returns | Signature | Note |
|---|---|---|---|
| `c` | node | `<no parentheses>` |  |
| `debug` | — | `([num operation, num/obj p2, num/obj p3])` | Acts like a breakpoint in Flexscript to start debugging |
| `delayednodefunction` | — | `(node thenode, double timeDelay[, par1, par2, ... par10])` |  |
| `eventlisten` | node | `(obj object, str eventName, node relayNode[, num flags, par1, par2, ... par10])` |  |
| `executefsnode` | — | `(node thenode, node c[, node i, node/num/str eventdata, int nobuild])` | Execute a node as Flexscript |
| `i` | node | `<no parentheses>` |  |
| `inheritcode` | — | `()` |  |
| `ip` | num | `<no parentheses>` |  |
| `msgparam` | var | `(num param)` | Within an OnMessage trigger, get the value of a message parameter |
| `msgsendingobject` | node | `()` | Within an OnMessage trigger, get a reference to the object sending the message |
| `nodefunction` | — | `(node thenode [, par1, par2, ... par10])` |  |
| `nodefunctionlisten` | node | `(node theNode, node relayNode[, num flags, par1, par2, ... par10])` |  |
| `param` | var | `(num index)` | Gets a parameter passed into a function |
| `parqty` | num | `()` |  |
| `selectall` | — | `()` |  |
| `thisclass` | node | `<no parentheses>` |  |

## Object Ports

| Command | Returns | Signature | Note |
|---|---|---|---|
| `closeallip` | — | `(obj object)` | Close all input ports of object |
| `closeallop` | — | `(obj object)` | Close all output ports of object |
| `closeip` | — | `(obj object, num inputportnum)` | Close one input port of object |
| `closeop` | — | `(obj object, num outputportnum)` | Close one output port of object |
| `cpcpno` | num | `(obj object, num port)` |  |
| `inputopen` | num | `(obj object)` | Returns 1 if the object's input is open, 0 otherwise |
| `ipavailable` | num | `(obj object, num inputportnum)` |  |
| `ipopen` | num | `(object object, num inputportnum)` |  |
| `ipopno` | num | `(obj object, num inputportnum)` | Output port number that is connected to inputportnum of object |
| `ipready` | num | `(obj object, num inputportnum)` |  |
| `numconnections` | num | `()` | Gets the number of server connections that are currently active |
| `opavailable` | num | `(obj object, num outputportnum)` |  |
| `openallip` | — | `(obj object)` | Open all input ports of object |
| `openallop` | — | `(obj object)` | Open all output ports of object |
| `openip` | — | `(obj object, num inputportnum)` | Open one input port of object |
| `openop` | — | `(obj object, num outputportnum)` | Open one output port of object |
| `opipno` | num | `(obj object, num outputportnum)` | Input port number that is connect to outputportnum of object |
| `opopen` | num | `(obj object, num outputportnum)` |  |
| `opready` | num | `(obj object, num outputportnum)` |  |
| `outputopen` | num | `(obj object)` | Returns 1 if the object's output is open, 0 otherwise |
| `transitionport` | num | `()` |  |

## Output

| Command | Returns | Signature | Note |
|---|---|---|---|
| `endoffile` | num | `()` | Returns 1 if the end of the currently open file has been reached |
| `fileclose` | — | `()` | Closes the FlexSim file interface |
| `fileexists` | num | `(str filepath)` | Returns 1 if the file exists, 0 otherwise |
| `fileopen` | — | `(str filename [, str opentype])` | Opens the FlexSim file interface |
| `filereadline` | str | `()` | Reads a line from the currently open file |
| `fpc` | — | `(num value)` | Prints an ASCII character to the open file |
| `fpd` | — | `(num value)` | Prints an integer value to the open file |
| `fpf` | — | `(num value)` | Prints a floating point value to the open file |
| `fpr` | — | `()` | Prints a return (new line) to the open file |
| `fpt` | — | `(str text)` | Prints a string to the open file |
| `mpd` | — | `(num value)` | Print an integer to the system console |
| `mpf` | — | `(num value)` | Print a floating point value to the system console |
| `mpr` | — | `()` | Print a new line to the system console |
| `mpt` | — | `(str printvalue)` | Print text to the system console |
| `outputtreecontent` | — | `(node containernode, str path+filename)` | Writes the data of all nodes in the tree under containernode to a text file |
| `print` | — | `([var value, var value2, ...])` | Print the contents of the given parameters and a new line to the console |
| `profileevents` | — | `()` | Prints descriptions of all events currently in the event queue to the output console |
| `report` | — | `(obj containerobj, str path+filename, num append [0/1])` |  |
| `reportmode` | — | `(int mode [0=general, 1=state])` |  |

## Rack Object

| Command | Returns | Signature | Note |
|---|---|---|---|
| `rackdrawfilledcell` | — | `(obj rack, num bay, num level, num x, num y, num z, num sx, num sy, num sz, num red, num green, num blue)` | Draws a box in the rack's given bay and level |
| `rackdrawvirtualcontent` | — | `(obj rack, num bayfillperc, num levelfillperc, num itemdepth, num red, num green, num blue, num virtualonly)` | Draw the rack's virtual content |
| `rackgetbaycontent` | num | `(obj rack, num bay)` | Gets the number of flow items in the bay of the rack |
| `rackgetbayloc` | num | `(obj rack, num bay)` | Gets the x location of bay relative to the rack's origin |
| `rackgetbayofitem` | num | `(obj rack, obj item)` | Gets the bay number of the rack that the item is placed in |
| `rackgetbaysize` | num | `(obj rack, num bay)` | Gets the x size of bay of the rack |
| `rackgetcellcontent` | num | `(obj rack, num bay, num level)` | Gets the number of flow items in bay and level of the rack |
| `rackgetcellvar` | num | `(obj rack, num bay, num level, num varnum, node storagenode)` |  |
| `rackgetitembybaylevel` | obj | `(obj rack, num bay, num level, num itemrank)` | Gets a reference to an item in the bay and level of the rack |
| `rackgetitemcellrank` | num | `(obj rack, obj item)` | Gets the item's rank within the rack's cell |
| `rackgetlevelloc` | num | `(obj rack, num bay, num level)` | Gets the y or z location of the level relative to the rack's origin |
| `rackgetlevelofitem` | num | `(obj rack, obj item)` | Gets the level number of the rack that the item is placed in |
| `rackgetlevelsize` | num | `(obj rack, num bay, num level)` | Gets the y or z size of the level of the rack |
| `rackgetnrofbays` | num | `(obj rack)` | Gets the number of bays rack has |
| `rackgetnroflevels` | num | `( obj rack [,num bay ] )` | Gets the number of levels in the bay of the rack |
| `rackrecycleitem` | — | `(obj rack, obj item, num binrank)` | Recycles the flowitem into the flowitem bin |
| `rackrestoreitem` | — | `(obj rack, num binrank, num bay, num level, num position)` | Restores a flowitem from the flowitem bin into the bay and level |
| `racksetcellcontent` | — | `(obj rack, num bay, num level, num contentval)` | Sets the virtual content of the rack's cell to contentval |
| `racksetcellvar` | — | `(obj rack,num bay, num level, num varnum, num setvalue, node storagenode)` |  |

## Statistics

| Command | Returns | Signature | Note |
|---|---|---|---|
| `addbundleentry` | num | `(node datanode[, field1val, field2val, ...])` | Adds a new entry to the bundle (base 0) |
| `addbundlefield` | num | `(node datanode, str fieldname, num type[, num maxstrsize])` | Adds a field to the bundle |
| `bernoulli` | num | `(num prob, num succeed-value, num fail-value[, num stream])` | Returns one of two possible values, based on the given probability |
| `beta` | num | `(num min, num max, num shape1, num shape2[, num stream])` | Returns a random sample from a beta distribution |
| `binomial` | num | `(num trials, num prob[, num stream])` | Returns a random sample from a binomial distribution |
| `calculateconfidence` | — | `(num stddev, num nrofdatapoints, num confidence)` |  |
| `cauchy` | num | `(num location, num scale[, num stream])` | Returns a random sample from a cauchy distribution |
| `cempirical` | num | `(str/node/num table[, num stream])` | Returns a random sample from a continuous empirical distribution |
| `clearbundle` | — | `(node datanode[, num resetflags = 0])` | Clears all bundle data |
| `dataplot` | — | `(obj tree, num type, num action, num x, num y, num z)` |  |
| `dempirical` | — | `(str/node/num table[, num stream])` | Returns a random sample from a discrete empirical distribution |
| `duniform` | — | `(num min, num max[, num stream])` | Returns random integer between the min and max |
| `empirical` | num | `(str/node/num table[, num stream])` |  |
| `erlang` | num | `(num location, num scale, num shape[, num stream])` | Returns a random sample from an erlang distribution |
| `exponential` | num | `(num location, num scale [, num stream])` | Returns a random sample from an exponential distribution |
| `exportdataset` | — | `(node datatree, str filename, num format[, str wrOption = "w"])` |  |
| `extremevalue1a` | num | `(num location, num scale[, num stream])` | Returns a random sample from a extremevalue1a distribution |
| `extremevalue1b` | num | `(num location, num scale[, num stream])` | Returns a random sample from a extremevalue1b distribution |
| `gamma` | num | `(num location, num scale, num shape[, num stream])` | Returns a random sample from a gamma distribution |
| `geometric` | — | `(num prob[, num stream])` | Returns a random sample from a geometric distribution |
| `getbundlefieldname` | str | `(node datanode, num fieldnr)` | Gets the name of the nth ranked field (base 0) in the bundle |
| `getbundlefieldnr` | num | `(node datanode, str fieldname)` | Gets the rank of the bundle field (base 0) |
| `getbundlefieldtype` | num | `(node datanode, num fieldnr)` | Gets the data type of the bundle field at rank fieldnr (base 0) |
| `getbundleindexentries` | — | `(node datanode, str/num field, str/num value)` | Gets all rows with a matching value in the given field |
| `getbundlenrentries` | num | `(node datanode)` | Gets the total number of entries in the bundle |
| `getbundlenrfields` | num | `(node datanode)` | Gets the total number of fields in the bundle |
| `getbundlevalue` | num/str/node | `(node datanode, num entrynr, str/num field)` | Gets the value of the defined entry (base 0) and field (base 0) of the bundle |
| `getdatastat` | num | `(num stat, num/node nrSamples/parentNode, num sampler [, num p1, num p2, num p3])` |  |
| `getstat` | var | `(node object, str name, int flag [, var p1, var p2, var p3])` | Returns the statistic value for the given object |
| `getstatetableutilization` | num | `(Table stateTable, TrackedVariable stateProfile)` |  |
| `getstream` | num | `(node object)` | Returns a unique random stream associated with the object |
| `graphtohistogram` | — | `(obj graph, obj histogram, num start, num end, num res, num xyformat)` |  |
| `hypergeometric` | num | `(num trials, num successes, num size[, num stream])` | Returns a random sample from a hypergeometric distribution |
| `importdataset` | — | `(node datatree, str filename, num format)` |  |
| `inversegaussian` | num | `(num location, num scale, num shape[, num stream])` | Returns a random sample from an inversegaussian distribution |
| `inverseweibull` | num | `(num location, num scale, num shape[, num stream])` | Returns a random sample from a inverseweibull distribution |
| `invertedweibull` | num | `(num location, num scale, num shape[, num stream])` | Returns a random sample from an inverted weibull distribution |
| `johnsonbounded` | num | `(num min, num max, num shape1, num shape2[, num stream])` | Returns a random sample from a johnson bounded distribution |
| `johnsonunbounded` | num | `(num location, num scale, num shape1, num shape2[, num stream])` | Returns a random sample from a johnson unbounded distribution |
| `laplace` | num | `(num location, num scale[, num stream])` | Returns a random sample from a laplace distribution |
| `logarithmic` | num | `(num theta[, num stream])` | Returns a random sample from a logarithmic distribution |
| `logistic` | num | `(num location, num scale[, num stream])` | Returns a random sample from a logistic distribution |
| `loglaplace` | num | `(num location, num scale, num shape[, num stream])` | Returns a random sample from a log laplace distribution |
| `loglogistic` | num | `(num location, num scale, num shape[, num stream])` | Returns a random sample from a log logistic distribution |
| `lognormal2` | num | `(num location, num scale, num shape[, num stream])` | Returns a random sample from a lognormal distribution |
| `makehisto` | — | `(obj datalist, obj histocontainer, num nrofbuckets)` |  |
| `negbinomial` | num | `(num successes, num prob[, num stream])` | Returns a random sample from a negative binomial distribution |
| `normal` | num | `(num mean, num stddev[, num stream])` | Returns a random sample from a normal distribution |
| `pareto` | num | `(num min, num scale[, num stream])` | Returns a random sample from a pareto distribution |
| `pearsont5` | num | `(num location, num scale, num shape[, num stream])` | Returns a random sample from a pearson t 5 distribution |
| `pearsont6` | num | `(num location, num scale, num shape1, num shape2[, num stream])` | Returns a random sample from a pearson t 6 distribution |
| `poisson` | num | `(num mean[, num stream])` | Returns a random sample from a poisson distribution |
| `qstudentt` | num | `(num confidence [0.9/0.95/0.99], num degreesoffreedom)` |  |
| `randantithetic` | — | `(num on/off)` |  |
| `randinit` | — | `(num stream, num seedvalue[, num seedvalue2, num seedvalue3, num seedvalue4, num seedvalue5, num seedvalue 6])` | Initializes a random stream |
| `randnext` | num | `(num stream)` |  |
| `randomwalk` | num | `(num location, num scale, num shape[, num stream])` |  |
| `randstate` | num | `(num stream[, seednr])` | Gets the current state value for the random number stream |
| `removebundleentry` | — | `(node datanode, int entrynum)` | Removes the entry entrynum (base 0) of the bundle |
| `removefirstbundleentry` | — | `(node datanode)` | Removes the first entry in the bundle |
| `removelastbundleentry` | — | `(node datanode)` | Removes the last entry of the bundle |
| `setbundlevalue` | — | `(node datanode, num entrynr, str/num field, num/str val)` | Sets the value of the entry (base 0) and field (base 0) of the bundle |
| `triangular` | num | `(num min, num max, num mode[, num stream])` | Returns a random sample from a triangular distribution |
| `uniform` | num | `(num min, num max[, num stream])` | Returns a random number between the min and max values |
| `weibull` | num | `(num location, num scale, num shape[, num stream])` | Returns a random sample from a weibull distribution |

## Strings

| Command | Returns | Signature | Note |
|---|---|---|---|
| `asciistr` | num | `(str string [, num index])` |  |
| `cdir` | str | `()` |  |
| `currentfile` | — | `()` | Gets the path to the currently open model |
| `documentsdir` | str | `()` | Gets the full path to the currently active user's FlexSim Projects folder in My Documents |
| `executestring` | — | `(str thestring[, node c, node i, obj eventdata])` | Executes a string as flexscript |
| `getdirbrowse` | — | `(str initialdir, str message, node result)` | Opens a browser window to pick a directory |
| `getpath` | str | `(str truncatedpath)` | Returns the full file path of the truncated path |
| `modeldir` | — | `()` | Gets the path to the currently open model's directory |
| `pdir` | str | `()` |  |
| `pfile` | str | `()` |  |
| `programdatadir` | str | `()` | Gets the full path to the directory where FlexSim stores program data |
| `replacedata_n` | num | `(obj startnode, num value, num replacement)` |  |
| `replacedata_s` | num | `(obj startnode, str string, str replacement[, num replication])` |  |
| `replacename` | num | `(obj startnode, str searchname, str replacement)` |  |
| `searchdata_n` | num | `(obj startnode, num val)` |  |
| `searchdata_s` | num | `(obj startnode, str string [, num replication])` |  |
| `searchname` | num | `(obj startnode, str searchname)` |  |
| `strascii` | str | `(num asciivalue)` |  |
| `stringtoken` | str | `(str token, str delimit)` | Finds the next token in a string |
| `tostring` | str | `(thing)` |  |
| `truncatemediapath` | str | `(str path)` |  |
| `userdatadir` | str | `()` | Gets the full path to the directory where FlexSim stores user data |

## Tables

| Command | Returns | Signature | Note |
|---|---|---|---|
| `exporttable` | — | `(node exportnode, str filename[, num exportcolumnnames = 0, num exportrownames = 0, str listseparator = 0, str decimalseparator = 0])` | Export a table in .CSV format |
| `importtable` | — | `(node importnode, str filename[, num importcolheaders = 0, num importrowheaders = 0, str listseparator = 0, str decimalseparator = 0, str thousandseparator = 0])` | Imports table data into importnode from a csv file. When importing both row and column headers, the first value in the f |

## TaskExecuter Object

| Command | Returns | Signature | Note |
|---|---|---|---|
| `addsphere` | — | `(obj object, num x, num y, num z, num radius)` |  |
| `changepersonvisuals` | — | `(treenode person, string category, num/string index [, Array color])` | Change the person's visuals (Head, Hair, Skin Texture, Clothes) |
| `createcoordinatedtasksequence` | node | `(obj dispatcher)` | Create a new, empty coordinated task sequence |
| `dispatchcoordinatedtasksequence` | — | `(obj tasksequence)` | Dispatches the coordinated task sequence |
| `distancetotravel` | num | `(obj traveler, obj destination)` | Gets the distance a traveler would need to travel to reach destination |
| `drawspheres` | — | `(obj object, obj view)` |  |
| `freeoperators` | — | `(obj taskexecuter, obj involved)` | Release all operators connected to taskexecuter that are working on involved |
| `getallocatedfromkey` | obj | `(obj coordtasksequence, num key)` | Gets a reference to the object allocated for the allocate task for the given key |
| `getcoordinatedtasksequence` | node | `(obj dispatcher, num tasksequencerank)` | Gets a reference to a coordinated task sequence |
| `getcurtask` | — | `(node tasksequence)` | Gets the current task number of tasksequence |
| `getpreempt` | num | `(node tasksequence)` | Gets the preempt value of the tasksequence |
| `getpriority` | num | `(node tasksequence)` | Gets the priority value of the tasksequence |
| `getutilizedobject` | obj | `(obj dispatcher, obj involved, obj station, num utilizednr)` | Gets a reference to an operator that is currently executing a utilize task |
| `insertallocatetask` | num | `(obj tasksequence, obj dispatcher, num priority, num preempt [, num noblock] )` | Inserts an allocate task into the coordinated task sequence |
| `insertdeallocatetask` | num | `(obj tasksequence, num allocationkey [, num noblock] )` | Inserts a deallocate task into the coordinated task sequence |
| `insertproxytask` | num | `(obj tasksequence, num executerkey, num type [, num/obj involved1, num/obj involved2 , num var1, num var2, num var3, num var4])` | Inserts a proxy task into the coordinated task sequence |
| `insertsynctask` | num | `(obj tasksequence, num taskkey)` | Inserts a sync task into the coordinated task sequence |
| `pickoffsetx` | num | `(obj station, obj item, obj loadingobject)` |  |
| `pickoffsety` | num | `(obj station, obj item, obj loadingobject)` |  |
| `pickoffsetz` | num | `(obj station, obj item, obj loadingobject)` |  |
| `placeoffsetx` | num | `(obj station, obj item, obj unloadingobject)` |  |
| `placeoffsety` | num | `(obj station, obj item, obj unloadingobject)` |  |
| `placeoffsetz` | num | `(obj station, obj item, obj unloadingobject)` |  |
| `profiletasksequence` | — | `(node tasksequence)` | Prints information on a task sequence to the output console |
| `reordertasks` | — | `(obj dispatcher)` | Causes the dispatcher to reorder all its task sequences according to its queue strategy |
| `requestoperators` | — | `(obj dispatcher, obj station, obj involved, num nrofops, num priority, num preempting)` |  |
| `rerankbesttask` | — | `(obj object)` |  |
| `setcollisioncheck` | — | `(obj taskexecuter, num checkstate [, num checkinterval])` | Sets the state and interval for collision checking on the taskexecuter |
| `setpreempt` | — | `(obj tasksequence, num newpreempt)` | Sets the preempt value of the tasksequence |
| `setpriority` | — | `(obj tasksequence, num newpriority)` | Sets the priority of the tasksequence |

## Tree Access

| Command | Returns | Signature | Note |
|---|---|---|---|
| `library` | node | `()` | Reference to the library node in the main tree |
| `maintree` | node | `()` | Reference to the main tree |
| `model` | node | `()` | Reference the model's root node |
| `views` | node | `()` | Reference to the views tree |

## Tree Node Data

| Command | Returns | Signature | Note |
|---|---|---|---|
| `$iter` | num/node | `(num index)` | See User Manual > Miscellaneous Concepts > SQL Queries |
| `checkdllconnection` | num | `(node dllnode, num force)` | Returns 1 if the node is properly connected with a dll function |
| `comparetrees` | num | `(obj tree1, obj tree2)` |  |
| `createcoupling` | node | `(node container1, node container2)` | Creates a pointer coupling inside the two container nodes |
| `enablecode` | — | `(node codenode [, num mode])` | Makes the node a code node |
| `findownerobject` | obj | `(node thenode)` |  |
| `getsdtvalue` | num/node/str | `(node sdtNode, str valName)` | For developer use |
| `nodebreak` | — | `(node a)` |  |
| `nodeinsertafter` | node | `(node targetnode)` | Creates a new node immediately after targetnode |
| `nodeinsertinto` | node | `(node containernode)` | ⚠ deprecated — use `treenode.subnodes.add` |
| `nodejoin` | — | `(node a, node b)` |  |
| `objectexists` | num | `(obj object)` | Check if an object exists |
| `ownerobject` | obj | `(node thenode)` | Gets the object that "owns" the executing node, usually the current object |
| `removeallcouplingreferences` | — | `(node thenode)` | Recursively breaks couplings from a node and its subnodes in the tree |
| `setsdtvalue` | node | `(node sdtNode, str valName, num/str/node toVal)` | For developer use |
| `swapnoderanks` | — | `(obj/node parentnode, num index1, num index2)` | Swap the ranks for two nodes in a parent node's sub-tree |
| `switch_cppfunc` | num | `(node thenode[, num off/on])` | Get or set the "C++" flag of thenode |
| `switch_destroyonreset` | num | `(obj thenode[, num off/on])` | Get or set the "destroy on reset" flag of the object |
| `switch_dllfunc` | num | `(node dllnode[, num on/off/get])` |  |
| `switch_expanded` | num | `(node thenode[, num off/on])` | Expand/collaps sub-level nodes of thenode |
| `switch_flexscript` | num | `(obj object[, num on/off/query])` |  |
| `switch_hashedlist` | num | `(obj thenode[, num off/on])` | Get or set the "Hashed List" flag of thenode |
| `switch_noselect` | num | `(obj object[, num off/on])` | Get or set the "No Select" flag of the object |
| `switch_orphancoupling` | num | `(obj thenode[, num off/on])` | Get or set the "orphan coupling" flag of the object |
| `switch_preservecoupling` | num | `(obj thenode[, num off/on])` | Get or set the "preserve coupling" flag of the object |
| `switch_protected` | num | `(obj object[, num off/on])` | Get or set the "Protected" flag of the object |
| `switch_selected` | num | `(obj thenode[, num off/on])` | Get or set the "Selected" flag of thenode |
| `switch_statelocked` | num | `(obj object[, num off/on])` |  |
| `switch_stateused` | num | `(obj thenode[, num off/on])` |  |

## User Interface

| Command | Returns | Signature | Note |
|---|---|---|---|
| `activateview` | — | `(obj viewnode)` |  |
| `activedocumentnode` | node | `()` | Gets the currently active document window |
| `activedocumentview` | num | `()` | Gets the currently active document window as an HWND |
| `activeview` | num | `()` | Gets the currently active window as an HWND |
| `addviewtext` | — | `(node view, string text)` |  |
| `applicationcommand` | — | `(str name[, p1, p2, p3, ...])` |  |
| `applyalleditstonodes` | — | `([obj view])` |  |
| `applyeditstonode` | — | `(obj view, obj editednode)` |  |
| `applylinks` | — | `(node view [, num refresh])` |  |
| `applytoedits` | — | `(obj view, obj editednode)` |  |
| `attlink` | node | `(node viewdatanode, str path)` |  |
| `beginaggregatedundo` | num | `(obj view, str description)` |  |
| `beginignoreundo` | — | `()` |  |
| `callwebscriptmethod` | — | `(obj view, str domid, str methodname, int type [, num/str data ...])` |  |
| `checkforexistingview` | node | `(obj view [, num mode])` |  |
| `clearundohistory` | — | `([obj view])` |  |
| `clickcode` | num | `()` |  |
| `closeview` | — | `(node viewnode)` |  |
| `codetotemplate` | — | `(obj code, obj edit)` |  |
| `codetotemplatetext` | — | `(node codeobject, node toobjecttext [, num firstcommentonly])` |  |
| `combocurrentnode` | node | `(obj combonode)` |  |
| `comborefresh` | — | `(obj combonode)` |  |
| `continueaggregatedundo` | num | `(obj view)` |  |
| `controlpressed` | num | `()` |  |
| `createpopup` | — | `(str viewpath, str objectfocus, str viewfocus, obj relativeto, num x, num y, num sx, num sy)` | Creates a new popup window |
| `createundorecord` | num | `(obj view, obj object, num undocode[, num p1, num p2, num p3])` |  |
| `createview` | node | `(str viewpath[, str objectfocus, str viewfocus])` |  |
| `cursorinfo` | num | `(obj/num view, num operation, num x/y/z, num pos/move)` |  |
| `deselectall` | — | `()` |  |
| `distcutoff` | — | `(num radius)` | Sets a globally applied cutoff radius for object drawing |
| `distfromviewpoint` | num | `(obj object, obj view)` | How far from the viewpoint object is in the window view |
| `draginfo` | — | `(num operation[, num val])` | Gets or set values related to draggin the mouse |
| `dropnodefrom` | node | `()` |  |
| `dropnodeto` | node | `()` |  |
| `dropx` | num | `()` | Gets the x position of a drop action |
| `dropy` | num | `()` | Gets the y position of a drop action |
| `dropz` | num | `()` | Gets the z position of a drop action |
| `endaggregatedundo` | — | `(obj view, num id)` |  |
| `endignoreundo` | — | `()` |  |
| `findundorecordbyid` | obj | `(obj view, num id)` |  |
| `finishundorecord` | — | `(obj view, num uniqueid)` |  |
| `getchecked` | num | `(obj window)` |  |
| `getcurrentundorecord` | obj | `(obj view[, int flags = 0x1])` |  |
| `getdashboardcontrol` | treenode | `(str id [, node startnode])` | Returns a reference to the dashboard control associated with id |
| `getfilebrowse` | str | `(str extension, str extensiondescription[, str defaultreturn, str location, num savemode, node filelist])` | Opens a standard windows file browser dialog window |
| `getfilesindirectory` | — | `(str directory, str extension, node destnode)` |  |
| `getparentwindow` | num | `(HWND window)` |  |
| `getpickingdrawfocus` | — | `(obj theview, num whattoget, num pickedid)` | Gets the picking draw focus |
| `getpickingmode` | num | `(obj view)` | Gets the picking mode for view |
| `getsystemmetric` | num | `(num metric[, HWND win])` |  |
| `gettableviewselection` | num | `(obj tableview, num val)` |  |
| `getundorecordid` | num | `(obj record)` |  |
| `getviewhwnd` | num | `(obj view, int windowtype)` | Gets the HWND handle for a certain window of a view |
| `getviewmenu` | var | `(obj view, int menutype)` |  |
| `getviewscale` | num | `(obj view)` |  |
| `getviewtext` | str | `(obj/num window)` |  |
| `highlightviewtext` | — | `(node view, num pos, num span, num mode span/lines)` | Highlights text in a code edit window |
| `iskeydown` | num | `([num key])` |  |
| `keyboardfocus` | num | `([HWND/node focus])` | Gets/Sets keyboard focus |
| `keyedclick` | — | `(obj object, num key[, obj view])` |  |
| `lastclickednode` | node | `()` |  |
| `lastkey` | num | `()` |  |
| `lastkeydown` | num | `()` |  |
| `lastkeyup` | num | `()` |  |
| `listboxrefresh` | — | `(obj listbox)` | Refreshes the contents of a listbox |
| `mainmenucommand` | num | `(str name)` |  |
| `makeactive` | — | `(obj viewwindow)` |  |
| `menubelow` | var | `(HMENU menu, num index)` |  |
| `menucheck` | — | `(HMENU menu, num index, num on/off)` |  |
| `menugray` | — | `(HMENU menu, num index, num on/off)` |  |
| `menumain` | var | `()` |  |
| `msg` | num | `(str caption, str text [, num buttontype])` | Display a message dialog |
| `nodefromwindow` | obj | `(num window)` |  |
| `ownerview` | obj | `(obj viewobj)` |  |
| `pickcolor` | — | `()` |  |
| `pickcolornode` | — | `(obj colornode)` |  |
| `pickedcolorblue` | num | `()` |  |
| `pickedcolorgreen` | num | `()` |  |
| `pickedcolorred` | num | `()` |  |
| `pickfile` | num | `(str extension, str extensiondescription[, str defaultreturn])` |  |
| `picklistapply` | — | `(obj combobox)` |  |
| `popupmenu` | — | `(obj view)` |  |
| `redirectconsole` | — | `(num console:output/system/compiler/trace, obj view)` | Redirects any prints to a given console |
| `redirectpicklists` | — | `(obj view)` |  |
| `redo` | — | `(obj view)` |  |
| `refreshview` | — | `(obj view)` |  |
| `repaintall` | — | `()` |  |
| `repaintallactive` | — | `()` |  |
| `repaintview` | — | `(obj view)` |  |
| `repositionhighlightededits` | — | `(obj view)` |  |
| `scrollinfo` | num | `(obj/HWND view, num get_set {0-1}, num horz_vert_ctl {0-2}, num scrollinfo {1-4}[, num value])` |  |
| `select` | num | `(obj window)` |  |
| `selectedobject` | node | `(obj window [, num reset])` |  |
| `selectedx` | num | `(obj window)` |  |
| `selectedy` | num | `(obj window)` |  |
| `selectedz` | num | `(obj window)` |  |
| `sendonactivatenotify` | — | `()` |  |
| `setcameradirection` | — | `(obj view, num x, num y, num z, num rx, num ry, num rz)` | Sets the viewpoint direction of the 3D view |
| `setcameravolume` | — | `(obj view, num w, num h, num nearclip, num farclip, num magnification, num fov, num projtype, num firstperson)` |  |
| `setchecked` | — | `(obj view, num value)` |  |
| `setcursor` | — | `(num cursorid[, num forgetCount = 0])` | Sets the visual look of the mouse pointer |
| `setdropcoords` | — | `(obj view[, obj dropnodefrom, obj dropnodeto])` |  |
| `setpickingdrawfocus` | — | `(obj theview, obj focus, num what[, obj secondaryfocus, num flags])` |  |
| `setselectedobject` | — | `(obj view, obj selected)` |  |
| `settableviewselection` | — | `(obj tableview, num startrow, num startcol, num endrow, num endcol[, num rangeselected])` |  |
| `setviewtext` | — | `(obj/HWND window, str text)` |  |
| `shiftpressed` | num | `()` |  |
| `showcompilerconsole` | — | `()` |  |
| `showhelp` | — | `(str topicname)` |  |
| `showhtmlhelp` | — | `(str topic)` |  |
| `standardpreopen` | — | `(obj object[, int nobuild])` |  |
| `sv` | node | `()` | Gets a reference to the last view desginated as sv() |
| `systemwindow` | num | `(num windowtype)` |  |
| `tabcontrolrefresh` | — | `(obj tabcontrolnode)` |  |
| `templatetocode` | — | `(obj code, obj edit)` |  |
| `undo` | — | `(obj view)` |  |
| `updateundorecord` | — | `(obj view, num uniqueid[, num updatetype, num p1, num p2])` |  |
| `viewdist` | num | `(obj view, obj object)` | Gets the distance from the focus point of view to object |
| `viewmenucommand` | — | `(str name, obj view)` |  |
| `webcommand` | — | `(str name[, p1, p2, p3, ...])` | Used to manipulate FlexSim's HTML engine/widgets |
| `windowflash` | — | `(HWND window, num count, num timeout)` |  |
| `windowfromnode` | num | `(obj viewnode)` |  |
| `windowgray` | — | `(HWND window, num graystate)` |  |
| `windowmenu` | num | `(obj view)` |  |
| `windowmove` | — | `(HWND window, num x, num y, num sx, num sy[, num refresh])` |  |
| `windowredraw` | — | `(HWND window, num x, num y, num sx, num sy, int flags)` |  |
| `windowshow` | — | `(HWND/node thewindow , num show)` |  |
| `windowstyle` | num | `(HWND/obj window [, num style, num useexstyle])` | Gets the windowstyle of window |

## Deprecated

| Command | Returns | Signature | Note |
|---|---|---|---|
| `acos` | num | `(num ratio)` | ⚠ deprecated — use `Math.acos` |
| `add` | num | `(num value1, num value2)` | ⚠ deprecated — use `the` |
| `addfullhistorymoverecord` | — | `(num flowitemid, num time, obj origin, obj destination, num itemtype)` | ⚠ deprecated — use `this` |
| `addfullhistorystaterecord` | — | `(num time, num state, obj object)` | ⚠ deprecated |
| `addlabel` | node | `(obj object, str labelname [, num/str value])` | ⚠ deprecated — use `treenode.LabelName` |
| `addtablecol` | — | `(str/num/node table [, num column, num datatype])` | ⚠ deprecated — use `Table.addCol` |
| `addtablerow` | — | `(str/num/node table [, num row, num datatype])` | ⚠ deprecated — use `Table.addRow` |
| `and` | num | `(num/node condition1, num/node condition2)` | ⚠ deprecated — use `the` |
| `angle` | num | `(num dx, num dy)` | ⚠ deprecated — use `Math.atan2` |
| `anode` | — | `(thing)` | ⚠ deprecated — use `tonode` |
| `anum` | — | `(thing)` | ⚠ deprecated — use `tonum` |
| `aptr` | — | `(thing)` | ⚠ deprecated — use `toptr` |
| `arraysize` | num | `(var array)` | ⚠ deprecated — use `Array.length` |
| `asin` | num | `(num ratio)` | ⚠ deprecated — use `Math.asin` |
| `assertattribute` | node | `(obj object, str attributename, num datatype)` | ⚠ deprecated — use `Object.attrs.assert` |
| `assertlabel` | node | `(obj object, str labelname, num datatype)` | ⚠ deprecated — use `treenode.labels.assert` |
| `assertsubnode` | node | `(node containernode, str name[, num datatype])` | ⚠ deprecated — use `treenode.subnodes.assert` |
| `atan` | num | `(num ratio)` | ⚠ deprecated — use `Math.atan` |
| `atan2` | num | `(num dy, num dx)` | ⚠ deprecated — use `Math.atan2` |
| `att` | — | `(obj object, num index)` | ⚠ deprecated — use `var` |
| `att_s` | — | `(obj object, str name)` | ⚠ deprecated — use `getvarnode` |
| `aviadd` | — | `(node viewnode)` | ⚠ deprecated — use `the` |
| `avifinish` | — | `(node viewnode)` | ⚠ deprecated — use `the` |
| `avistart` | — | `(str filename, node viewnode, num fps)` | ⚠ deprecated — use `the` |
| `bcgetitemkinematics` | node | `(obj basicconveyor, obj item, num kinematictype {0-main kinematic : 1-time dependent : 2-convey dependent})` | ⚠ deprecated — use `bcgetitemkinematics` |
| `bcgetitemposition` | — | `(obj basicconveyor, obj item)` | ⚠ deprecated |
| `bcsetdirection` | num | `(obj basicconveyor, num direction)` | ⚠ deprecated |
| `bcsetitemconveystate` | — | `(obj basicconveyor, obj item, num startpoint, num startspeed, num targetspeed, num accdec)` | ⚠ deprecated — use `the` |
| `bitwiseand` | num | `(num a, num b)` | ⚠ deprecated |
| `bitwisenot` | num | `(num a)` | ⚠ deprecated |
| `bitwiseor` | num | `(num a, num b)` | ⚠ deprecated |
| `bitwisexor` | num | `(num a, num b)` | ⚠ deprecated |
| `ceil` | num | `(num num)` | ⚠ deprecated — use `Math.ceil` |
| `cell` | node | `(node table, num col, num row)` | ⚠ deprecated — use `Table.cell` |
| `cellrc` | node | `(obj object, num row, num col)` | ⚠ deprecated — use `Table.cell` |
| `cellrowcolumn` | node | `(node table, num row, num col)` | ⚠ deprecated — use `the` |
| `centerobject` | obj | `(obj object, num centerportnum)` | ⚠ deprecated — use `Object.centerObjects` |
| `changeconveyorspeed` | — | `(obj conveyor, num newspeed )` | ⚠ deprecated — use `Conveyor.targetSpeed` |
| `changetask` | — | `(obj tasksequence, num taskrank, num type [, obj involved1, obj involved2 , num var1, num var2, num var3, num var4] )` | ⚠ deprecated — use `the` |
| `clearconsole` | — | `()` | ⚠ deprecated |
| `clearcontents` | — | `(node thenode)` | ⚠ deprecated — use `treenode.subnodes.clear` |
| `clearfullhistory` | — | `(int resetuniqueid)` | ⚠ deprecated |
| `clearglobaltable` | — | `(str/node/num table)` | ⚠ deprecated — use `the` |
| `closeinput` | — | `(obj object)` | ⚠ deprecated — use `Object.input.close` |
| `closeoutput` | — | `(obj object)` | ⚠ deprecated — use `Object.output.close` |
| `coloraqua` | — | `(obj object)` | ⚠ deprecated |
| `colorarray` | — | `(obj object, num colornumber)` | ⚠ deprecated |
| `colorblack` | — | `(obj object)` | ⚠ deprecated |
| `colorblue` | — | `(obj object)` | ⚠ deprecated |
| `colorbrown` | — | `(obj object)` | ⚠ deprecated |
| `colorgray` | — | `(obj object)` | ⚠ deprecated |
| `colorgreen` | — | `(obj object)` | ⚠ deprecated |
| `colorlightblue` | — | `(obj object)` | ⚠ deprecated |
| `colorlime` | — | `(obj object)` | ⚠ deprecated |
| `colororange` | — | `(obj object)` | ⚠ deprecated |
| `colorpink` | — | `(obj object)` | ⚠ deprecated |
| `colorpurple` | — | `(obj object)` | ⚠ deprecated |
| `colorrandom` | — | `(obj object)` | ⚠ deprecated |
| `colorred` | — | `(obj object)` | ⚠ deprecated |
| `colorrgb` | — | `(obj object, num red, num green, num blue)` | ⚠ deprecated |
| `colorsilver` | — | `(obj object)` | ⚠ deprecated |
| `colorteal` | — | `(obj object)` | ⚠ deprecated |
| `colorwhite` | — | `(obj object)` | ⚠ deprecated |
| `coloryellow` | — | `(obj object)` | ⚠ deprecated |
| `comparetext` | num | `(str text1, str text2[, int ignorecase])` | ⚠ deprecated — use `the` |
| `concat` | str | `(str text1, str text2 [, str text3, ...])` | ⚠ deprecated — use `the` |
| `connect` | — | `(obj object1, num port1, obj object2, num port2)` | ⚠ deprecated — use `contextdragconnection` |
| `containerentry` | — | `(obj container, obj item)` | ⚠ deprecated — use `tracked` |
| `containerexit` | — | `(obj container, obj item)` | ⚠ deprecated — use `tracked` |
| `content` | num | `(node parent)` | ⚠ deprecated — use `treenode.subnodes.length` |
| `contentobj` | num | `(obj container)` | ⚠ deprecated — use `Object.attrs.length` |
| `cos` | num | `(num angle)` | ⚠ deprecated — use `Math.cos` |
| `count` | — | `()` | ⚠ deprecated |
| `cppgettoken` | — | `(obj textblock, num tokenindex)` | ⚠ deprecated |
| `cppsettoken` | — | `(obj textblock, num tokenindex, str insertiontext)` | ⚠ deprecated |
| `createcopy` | node | `(node thenode, node container [, num samename, num inobject, num cached, num replace])` | ⚠ deprecated — use `treenode.copy` |
| `createdelaytask` | — | `(obj dispatcher, num delaytime, num state, num priority, num preempting)` | ⚠ deprecated — use `Process` |
| `createemptytasksequence` | node | `(obj dispatcher, num priority, num preempting)` | ⚠ deprecated — use `TaskSequence.create` |
| `createglobaltasksequence` | node | `(str name, obj dispatcher [, obj dp1, obj dp2, obj dp3, obj dp4, obj dp5])` | ⚠ deprecated — use `Process` |
| `createinstance` | obj | `(obj class, obj location)` | ⚠ deprecated — use `Object.create` |
| `createloadtask` | — | `(obj taskexecuter, obj destination, obj flowitem, num priority, num preempting)` | ⚠ deprecated — use `Process` |
| `createsendmessagetask` | — | `(obj taskexecuter, obj receiver, num priority, num preempting , num param1, num param2, num param3)` | ⚠ deprecated — use `Process` |
| `createstandardtask` | — | `(obj taskexecuter, obj origin, obj destination, obj flowitem, num priority, num preempting)` | ⚠ deprecated — use `Process` |
| `createtravelloadtask` | — | `(obj taskexecuter, obj destination, obj flowitem, num priority, num preempting)` | ⚠ deprecated — use `Process` |
| `createtraveltask` | — | `(obj taskexecuter, obj destination, num priority, num preempting)` | ⚠ deprecated — use `Process` |
| `createtraveltolocandwaittask` | — | `(obj taskexecuter, num relativeloc, num x, num y, num z, num endspeed, num state, num priority, num preempting)` | ⚠ deprecated — use `Process` |
| `createtraveltoloctask` | — | `(obj dispatcher, num relativeloc, num x, num y, num z, num endspeed, num priority, num preempting)` | ⚠ deprecated — use `Process` |
| `createtravelunloadtask` | — | `(obj taskexecuter, obj destination, obj flowitem, num priority, num preempting)` | ⚠ deprecated — use `Process` |
| `createunloadtask` | — | `(obj dispatcher, obj destination, obj flowitem, num priority, num preempting)` | ⚠ deprecated — use `Process` |
| `createwaittask` | — | `(obj taskexecuter, num state, num priority, num preempting)` | ⚠ deprecated — use `Process` |
| `current` | num/node | `<no parentheses>` | ⚠ deprecated — use `current` |
| `dbchangetable` | — | `(str tablename)` | ⚠ deprecated — use `Database.Connection` |
| `dbclose` | — | `()` | ⚠ deprecated — use `Database.Connection` |
| `dbexportnode` | — | `(obj tablenode, num startrow, num startcol, num numrows, num numcols)` | ⚠ deprecated — use `Database.Connection` |
| `dbexporttable` | — | `(str tablename, num startrow, num startcol, num numrows, num numcols)` | ⚠ deprecated — use `Database.Connection` |
| `dbgetfieldname` | str | `(num column)` | ⚠ deprecated — use `Database.Connection` |
| `dbgetmetrics` | num | `()` | ⚠ deprecated — use `Database.Connection` |
| `dbgetmode` | num | `()` | ⚠ deprecated — use `Database.Connection` |
| `dbgetnumcols` | num | `()` | ⚠ deprecated — use `Database.Connection` |
| `dbgetnumrows` | num | `()` | ⚠ deprecated — use `Database.Connection` |
| `dbgettablecell` | str | `(num row, num col)` | ⚠ deprecated — use `Database.Connection` |
| `dbgettablenum` | num | `(num row, num col)` | ⚠ deprecated — use `Database.Connection` |
| `dbgettablestr` | str | `(num row, num col)` | ⚠ deprecated — use `Database.Connection` |
| `dbimportnode` | — | `(obj tablenode, num startrow, num startcol, num numrows, num numcols)` | ⚠ deprecated — use `Database.Connection` |
| `dbimporttable` | — | `(str tablename, num startrow, num startcol, num numrows, num numcols)` | ⚠ deprecated — use `Database.Connection` |
| `dbopen` | num | `(str databasename, str command {SQL string or Tablename}, num usetable [, num showlogin])` | ⚠ deprecated — use `Database.Connection` |
| `dbsettablecell` | — | `(num row, num col, str value)` | ⚠ deprecated — use `Database.Connection` |
| `dbsettablenum` | — | `(num row, num col, num val)` | ⚠ deprecated — use `Database.Connection` |
| `dbsettablestr` | — | `(num row, num col, str val)` | ⚠ deprecated — use `Database.Connection` |
| `dbsqlquery` | — | `(str query[, num forwardOnlyCursor])` | ⚠ deprecated — use `Database.Connection` |
| `dbusername` | — | `(str username, str password)` | ⚠ deprecated — use `Database.Connection` |
| `dbviewtable` | — | `()` | ⚠ deprecated — use `Database.Connection` |
| `dbwritechanges` | — | `()` | ⚠ deprecated — use `Database.Connection` |
| `defaultdragconnection` | — | `(obj fromobject, obj toobject, char characterpressed)` | ⚠ deprecated — use `contextdragconnection` |
| `degreestoradians` | num | `(num degrees)` | ⚠ deprecated — use `Math.radians` |
| `deletetablecol` | — | `(str/num/node table, num column)` | ⚠ deprecated — use `Table.deleteCol` |
| `deletetablerow` | — | `(str/num/node table, num row)` | ⚠ deprecated — use `Table.deleteRow` |
| `dellabel` | — | `(obj object, str/num label)` | ⚠ deprecated — use `treenode.labels` |
| `destroyobject` | — | `(node thenode)` | ⚠ deprecated — use `treenode.destroy` |
| `diffsign` | num | `(num value1, num value2)` | ⚠ deprecated — use `Math.sign` |
| `dispatchtasksequence` | — | `(obj tasksequence)` | ⚠ deprecated — use `TaskSequence.dispatch` |
| `div` | num | `(num value1, num value2)` | ⚠ deprecated — use `the` |
| `divide` | num | `(num value1, num value2)` | ⚠ deprecated |
| `dumpquery` | — | `(node dumpNode[, num asTable])` | ⚠ deprecated — use `Table.query` |
| `enablefullhistory` | — | `(num on/off)` | ⚠ deprecated — use `the` |
| `endwaittask` | — | `(obj taskexecuter)` | ⚠ deprecated — use `Process` |
| `eq` | num | `(num/node value1, num/node value2)` | ⚠ deprecated — use `the` |
| `excelexportnode` | — | `(node table, num startrow, num startcol, num numrows, num numcols [, num rowchar, num colchar])` | ⚠ deprecated — use `excelexporttable` |
| `excelimportnode` | — | `(obj node, num startrow, num startcol, num numrows, num numcols [, num rowchar, num colchar])` | ⚠ deprecated — use `excelimporttable` |
| `excelmacro` | — | `(str macro)` | ⚠ deprecated |
| `excelrangeread` | — | `(str rangename[treenode target], num row, num col[, num numRows, num numCols, num flags])` | ⚠ deprecated — use `excelrangereadnum` |
| `excelrangewrite` | — | `(treenode source, num row, num col, num flags)` | ⚠ deprecated — use `of` |
| `excelread` | — | `(num row, num col)` | ⚠ deprecated — use `excelreadnum` |
| `excelwrite` | — | `(str data, num row, num col)` | ⚠ deprecated — use `excelwritenum` |
| `executetablecell` | — | `(str/node/num table, num row, num column)` | ⚠ deprecated — use `Table.executeCell` |
| `exp` | num | `(num value)` | ⚠ deprecated — use `Math.exp` |
| `exportfullhistory` | num | `([str filename, num scenario, num replication])` | ⚠ deprecated |
| `fabs` | num | `(num value)` | ⚠ deprecated — use `Math.fabs` |
| `factorial` | num | `(num value)` | ⚠ deprecated — use `Math.factorial` |
| `familycreate` | — | `(obj familycontainer, str name)` | ⚠ deprecated — use `the` |
| `familydelete` | — | `(obj familycontainer, str name)` | ⚠ deprecated — use `the` |
| `familymember` | node | `(obj familycontainer, str name, num index)` | ⚠ deprecated — use `the` |
| `familymemberadd` | — | `(obj familycontainer, str name, obj member)` | ⚠ deprecated — use `the` |
| `familymemberdel` | — | `(obj familycontainer, str name, obj member)` | ⚠ deprecated — use `the` |
| `familymemberqty` | num | `(obj familycontainer, str name)` | ⚠ deprecated — use `the` |
| `fillarray` | — | `(array thearray, num/treenode/string val1[, num/treenode/string val2, ...])` | ⚠ deprecated — use `the` |
| `findmatch` | — | `(num nr, num matchexpression[, returnexpression = count, num reverseorder = 0])` | ⚠ deprecated |
| `findmax` | — | `(num nr, num valueexpression[, returnexpression = maxvalue, num criterion = true, num reverseorder = 0])` | ⚠ deprecated |
| `findmin` | — | `(num nr, num valueexpression[, returnexpression = maxvalue, num criterion = true, num reverseorder = 0])` | ⚠ deprecated |
| `first` | node | `(node container)` | ⚠ deprecated — use `treenode.first` |
| `floor` | num | `(num num)` | ⚠ deprecated — use `Math.floor` |
| `fmod` | num | `(num value1, num value2)` | ⚠ deprecated — use `Math.fmod` |
| `forobjecthighlightedin` | — | `(obj view) { }` | ⚠ deprecated — use `selectedobject` |
| `forobjectlayerunder` | — | `(node container) { }` | ⚠ deprecated — use `for` |
| `forobjectsbefore` | — | `(node thenode) { }` | ⚠ deprecated — use `for` |
| `forobjectsbehind` | — | `(node thenode) { }` | ⚠ deprecated — use `for` |
| `forobjectselectionset` | — | `(node container) { }` | ⚠ deprecated |
| `frac` | num | `(num value)` | ⚠ deprecated — use `Math.frac` |
| `ge` | num | `(num value1, num value2)` | ⚠ deprecated — use `the` |
| `get` | num | `(node thenode)` | ⚠ deprecated — use `treenode.value` |
| `getcolorcomponent` | num | `(obj object, num index)` | ⚠ deprecated — use `Color.r` |
| `getdatatype` | num | `(thing)` | ⚠ deprecated — use `treenode.dataType` |
| `getinput` | num | `(obj involved)` | ⚠ deprecated — use `Object.stats.input.value` |
| `getitemtype` | num | `(obj object)` | ⚠ deprecated — use `labels` |
| `getlabel` | num/str/node | `(obj node, str/num label)` | ⚠ deprecated — use `treenode.labelProperties` |
| `getlabelnum` | num | `(obj node, str/num label)` | ⚠ deprecated — use `treenode.labelProperties` |
| `getlabelstr` | str | `(obj node, str/num label)` | ⚠ deprecated — use `treenode.labelProperties` |
| `getmodelunitnum` | num | `(num querytype)` | ⚠ deprecated — use `getmodelunit` |
| `getmodelunitstr` | str | `(num querytype)` | ⚠ deprecated — use `getmodelunit` |
| `getmoverecord` | — | `(num record)` | ⚠ deprecated |
| `getmoverecordvalue` | num | `(num record, num value)` | ⚠ deprecated |
| `getname` | str | `(node thenode)` | ⚠ deprecated — use `treenode.name` |
| `getnodename` | str | `(node thenode)` | ⚠ deprecated — use `treenode.name` |
| `getnodenum` | num | `(node thenode)` | ⚠ deprecated — use `treenode.value` |
| `getnodestr` | str | `(node thenode)` | ⚠ deprecated — use `treenode.value` |
| `getnroftasks` | num | `(obj tasksequence)` | ⚠ deprecated — use `the` |
| `getnummoverecords` | num | `()` | ⚠ deprecated |
| `getnumstaterecords` | num | `()` | ⚠ deprecated |
| `getobjectimageindex` | num | `(obj object)` | ⚠ deprecated |
| `getoutput` | num | `(obj involved)` | ⚠ deprecated — use `Object.stats.output.value` |
| `getquerycolcount` | num | `()` | ⚠ deprecated — use `Table.query` |
| `getquerymatchcount` | num | `()` | ⚠ deprecated — use `Table.query` |
| `getquerymatchtablerow` | num | `(str/num table, num matchRow)` | ⚠ deprecated — use `Table.query` |
| `getqueryvalue` | num/str/node | `(num row, num/str col)` | ⚠ deprecated — use `Table.query` |
| `getrank` | num | `(node thenode)` | ⚠ deprecated — use `treenode.rank` |
| `gets` | str | `(node thenode)` | ⚠ deprecated — use `treenode.value` |
| `getstatenum` | num | `(obj object [, num stateprofilenr])` | ⚠ deprecated — use `Object.stats.state` |
| `getstaterecord` | — | `(num record)` | ⚠ deprecated |
| `getstaterecordvalue` | num | `(num record, num value)` | ⚠ deprecated |
| `getstatestr` | str | `(obj object)` | ⚠ deprecated — use `Object.stats.state` |
| `gettablecell` | node | `(str/node/num table, num row, num column)` | ⚠ deprecated — use `Table.cell` |
| `gettablecols` | num | `(str/node/num table)` | ⚠ deprecated — use `Table.numCols` |
| `gettableheader` | str | `(string/num/node table, num row/col, num rowcolnr)` | ⚠ deprecated — use `Table.getColHeader` |
| `gettablenum` | num | `(str/node/num table, num row, num col)` | ⚠ deprecated — use `the` |
| `gettablerows` | num | `(str/node/num table)` | ⚠ deprecated — use `Table.numRows` |
| `gettablestr` | str | `(str/node/num table, num row, num col)` | ⚠ deprecated — use `the` |
| `gettaskinvolved` | node | `(obj tasksequence, num taskrank, num involvednum)` | ⚠ deprecated — use `the` |
| `gettasksequence` | node | `(obj dispatcher, num tasksequencerank)` | ⚠ deprecated — use `the` |
| `gettasksequencequeue` | node | `(obj dispatcher)` | ⚠ deprecated — use `the` |
| `gettaskstate` | num | `(obj tasksequence, num taskrank)` | ⚠ deprecated — use `the` |
| `gettasktype` | num | `(obj tasksequence, num taskrank)` | ⚠ deprecated — use `the` |
| `gettaskvariable` | num | `(obj tasksequence, num taskrank, num variablenum)` | ⚠ deprecated — use `the` |
| `gettotalnroftasks` | num | `(obj tasksequence)` | ⚠ deprecated — use `the` |
| `gettrackedvariable` | num | `(str/node trackedvar [, num valtype] )` | ⚠ deprecated — use `the` |
| `getvartype` | num | `(var)` | ⚠ deprecated — use `Variant.type` |
| `glBegin` | — | `(num primitive)` | ⚠ deprecated — use `the` |
| `glColor3d` | — | `(num red {0-1}, num green {0-1}, num blue {0-1})` | ⚠ deprecated — use `fglColor` |
| `glColor4d` | — | `(num red {0-1}, num green {0-1}, num blue {0-1}, num alpha {0-1})` | ⚠ deprecated — use `fglColor` |
| `glDisable` | — | `(num setting)` | ⚠ deprecated — use `fglDisable` |
| `glEnable` | — | `(num setting)` | ⚠ deprecated — use `fglEnable` |
| `glEnd` | — | `()` | ⚠ deprecated — use `the` |
| `glLoadIdentity` | — | `()` | ⚠ deprecated — use `fglLoadIdentity` |
| `glMatrixMode` | — | `(num mode)` | ⚠ deprecated — use `fglMatrixMode` |
| `glNormal3d` | — | `(num x, num y, num z)` | ⚠ deprecated — use `fglNormal` |
| `globallist` | obj | `(str listName)` | ⚠ deprecated — use `List` |
| `glPopAttrib` | — | `()` | ⚠ deprecated |
| `glPopMatrix` | — | `()` | ⚠ deprecated — use `fglPopMatrix` |
| `glPushAttrib` | — | `(num attribsetting)` | ⚠ deprecated |
| `glPushMatrix` | — | `()` | ⚠ deprecated — use `fglPushMatrix` |
| `glRotated` | — | `(num degrees, num x, num y, num z)` | ⚠ deprecated — use `fglRotate` |
| `glScaled` | — | `(num x, num y, num z)` | ⚠ deprecated — use `fglScale` |
| `glTexCoord2d` | — | `(num x 0-1, num y 0-1)` | ⚠ deprecated — use `the` |
| `glTranslated` | — | `(num x, num y, num z)` | ⚠ deprecated — use `fglTranslate` |
| `glVertex3d` | — | `(num x, num y, num z)` | ⚠ deprecated — use `the` |
| `groupaddmember` | num | `(str group, obj member)` | ⚠ deprecated — use `Group.addMember` |
| `groupmember` | obj | `(str group, num member)` | ⚠ deprecated — use `Group` |
| `groupnummembers` | num | `(str group)` | ⚠ deprecated — use `Group.length` |
| `groupremovemember` | num | `(str group, num/obj member)` | ⚠ deprecated — use `Group.removeMember` |
| `gt` | num | `(num value1, num value2)` | ⚠ deprecated — use `the` |
| `helpcommands` | — | `(node destnode, num what, str command)` | ⚠ deprecated |
| `hidelabel` | — | `(obj object[, num off/on])` | ⚠ deprecated — use `switch_hidelabel` |
| `hideports` | — | `(obj object[, num off/on])` | ⚠ deprecated — use `switch_hideconnectors` |
| `inc` | num | `(node thenode, num amount)` | ⚠ deprecated |
| `inittrackedvariable` | — | `(node theNode, num type[, num startValue, num flags])` | ⚠ deprecated — use `the` |
| `inobject` | obj | `(obj object, num inputportnum)` | ⚠ deprecated — use `Object.inObjects` |
| `inserttask` | — | `(obj tasksequence, num type [, obj involved1, obj involved2 , num var1, num var2, num var3, num var4] )` | ⚠ deprecated — use `TaskSequence.addTask` |
| `installpackage` | — | `(str path+filename)` | ⚠ deprecated — use `user` |
| `item` | num/node | `<no parentheses>` | ⚠ deprecated — use `item` |
| `iterate` | — | `(num start, num end, num step) {}` | ⚠ deprecated — use `for` |
| `label` | node | `(obj object, str/num label)` | ⚠ deprecated — use `treenode.labels` |
| `last` | node | `(node container)` | ⚠ deprecated — use `treenode.last` |
| `le` | num | `(num value1, num value2)` | ⚠ deprecated — use `the` |
| `listbackorders` | node | `(obj list[, var partitionId])` | ⚠ deprecated — use `List.backOrders` |
| `listentries` | node | `(obj list[, var partitionId])` | ⚠ deprecated — use `List.entries` |
| `listinlist` | num | `(num qty, num val [, num a,...])` | ⚠ deprecated — use `the` |
| `listmax` | num | `(num qty, num val [, num a,...])` | ⚠ deprecated — use `the` |
| `listmaximum` | num | `(num qty, num val [, num a,...])` | ⚠ deprecated — use `the` |
| `listmean` | num | `(num qty, num val [, num a,...])` | ⚠ deprecated — use `the` |
| `listmin` | num | `(num qty, num val [, num a,...])` | ⚠ deprecated — use `the` |
| `listminimum` | num | `(num qty, num val [, num a,...])` | ⚠ deprecated — use `the` |
| `listmul` | num | `(num qty, num val [, num a,...])` | ⚠ deprecated — use `the` |
| `listpull` | node/str/num/array | `(str/obj list, str/node query, num requestNum [, num requireNum, str/node/num puller, str/node/num partitionId, num flags])` | ⚠ deprecated — use `List.pull` |
| `listpush` | node/str/num/array | `(str/obj list [, str/node/num pushVal, str/node/num partitionId, ...])` | ⚠ deprecated — use `List.push` |
| `listremove` | — | `(obj entryOrBackOrder)` | ⚠ deprecated — use `List.backOrders` |
| `listsub` | num | `(num qty, num val [, num a,...])` | ⚠ deprecated — use `the` |
| `listsum` | num | `(num qty, num val [, num a,...])` | ⚠ deprecated — use `the` |
| `loadsound` | num | `(str path+filename)` | ⚠ deprecated — use `soundload` |
| `local` | node | `(num index)` | ⚠ deprecated — use `local` |
| `localarray` | — | `(num qty[, num datatype])` | ⚠ deprecated — use `the` |
| `log` | num | `(num value)` | ⚠ deprecated — use `Math.log` |
| `log10` | num | `(num value)` | ⚠ deprecated — use `Math.log10` |
| `lognormal` | num | `(num location, num normalmean, num normalstdev[, num stream])` | ⚠ deprecated — use `the` |
| `lognormalmeanstdev` | num | `(num mean, num stdev[, num stream])` | ⚠ deprecated |
| `lt` | num | `(num value1, num value2)` | ⚠ deprecated — use `the` |
| `main` | — | `()` | ⚠ deprecated — use `maintree` |
| `makearray` | — | `(num qty)` | ⚠ deprecated — use `Array` |
| `max` | num | `(num value1, num value2)` | ⚠ deprecated — use `Math.max` |
| `maxof` | num | `(num value1, num value2)` | ⚠ deprecated — use `Math.max` |
| `min` | num | `(num value1, num value2)` | ⚠ deprecated — use `Math.min` |
| `minof` | num | `(num value1, num value2)` | ⚠ deprecated — use `Math.min` |
| `module` | — | `(num type, node libnode)` | ⚠ deprecated — use `the` |
| `movetablecol` | — | `(str/num/node table, num column, num newcolumn)` | ⚠ deprecated — use `Table.moveCol` |
| `movetablerow` | — | `(str/num/node table, num row, num newrow)` | ⚠ deprecated — use `Table.moveRow` |
| `movetasksequence` | — | `(obj tasksequence, obj dispatcher)` | ⚠ deprecated — use `TaskSequence.move` |
| `mul` | num | `(num value1, num value2)` | ⚠ deprecated — use `the` |
| `multisorttable` | — | `(str/node/num table, num col1[, num col2, num col3, ...])` | ⚠ deprecated — use `Table.sort` |
| `nametomodelpath` | — | `(obj node, obj object)` | ⚠ deprecated — use `treenode.getPath` |
| `ncols` | num | `(node thenode)` | ⚠ deprecated — use `Table.numCols` |
| `ne` | num | `(num/node value1, num/node value2)` | ⚠ deprecated — use `the` |
| `next` | node | `(obj object)` | ⚠ deprecated — use `treenode.next` |
| `node` | node | `(str relativepath[, obj startnode])` | ⚠ deprecated — use `treenode.find` |
| `nodeadddata` | node | `(node thenode, num type:num/str/ptr/obj)` | ⚠ deprecated — use `treenode.value` |
| `nodedeldata` | node | `(node thenode)` | ⚠ deprecated — use `treenode.value` |
| `nodepoint` | node | `(node a, node b)` | ⚠ deprecated — use `treenode.value.` |
| `nodetomodelpath` | str | `(node thenode [, num nameflag=1])` | ⚠ deprecated — use `treenode.getPath` |
| `nodetopath` | str | `(node thenode [, num nameflag=0])` | ⚠ deprecated — use `treenode.getPath` |
| `not` | num | `(num/node value1)` | ⚠ deprecated — use `the` |
| `nrcp` | num | `(obj object)` | ⚠ deprecated — use `Object.centerObjects.length` |
| `nrip` | num | `(obj object)` | ⚠ deprecated — use `Object.inObjects.length` |
| `nrop` | num | `(obj object)` | ⚠ deprecated — use `Object.outObjects.length` |
| `nrows` | num | `(node thenode)` | ⚠ deprecated — use `Table.numRows` |
| `ntravelto` | — | `(obj traveler, obj spline, num endx, num endy, num endz, num vmax, num acc, num dec, num startspeed, num endspeed)` | ⚠ deprecated — use `kinematics` |
| `ntraveltoupdate` | — | `(obj object)` | ⚠ deprecated — use `kinematics` |
| `numtostring` | str | `(num value[, num width, num precision])` | ⚠ deprecated — use `string.fromNum` |
| `objectconnect` | — | `(obj object1, obj object2)` | ⚠ deprecated — use `contextdragconnection` |
| `objectshapecode` | num | `(obj object)` | ⚠ deprecated — use `getshapeindex` |
| `objecttexcode` | num | `(obj object)` | ⚠ deprecated — use `getobjecttextureindex` |
| `openflexsimchart` | — | `([str filename1] [,str filename2])` | ⚠ deprecated |
| `openglcolor` | — | `(num red {0-1}, num green {0-1}, num blue {0-1}, num opacity {0-1})` | ⚠ deprecated — use `fglColor` |
| `opengllighting` | — | `(num on/off)` | ⚠ deprecated — use `fglEnable` |
| `openinput` | — | `(obj object)` | ⚠ deprecated — use `Object.input.open` |
| `openoutput` | — | `(obj object)` | ⚠ deprecated — use `Object.output.open` |
| `or` | num | `(num/node condition1, num/node condition2)` | ⚠ deprecated — use `the` |
| `outobject` | obj | `(obj object, num outputportnum)` | ⚠ deprecated — use `Object.outObjects` |
| `parg` | — | `()` | ⚠ deprecated |
| `parnode` | node | `(num index)` | ⚠ deprecated — use `param` |
| `parstr` | str | `(num index)` | ⚠ deprecated — use `param` |
| `particleset` | — | `()` | ⚠ deprecated |
| `parval` | num | `(num index)` | ⚠ deprecated — use `param` |
| `pc` | — | `(num asciivalue)` | ⚠ deprecated — use `the` |
| `pd` | — | `(num value)` | ⚠ deprecated — use `the` |
| `pf` | — | `(num value)` | ⚠ deprecated — use `the` |
| `pi` | num | `()` | ⚠ deprecated — use `Math.pi` |
| `port` | num/node | `<no parentheses>` | ⚠ deprecated — use `port` |
| `pow` | num | `(num num1, num num2)` | ⚠ deprecated — use `Math.pow` |
| `pr` | — | `()` | ⚠ deprecated — use `the` |
| `prev` | node | `(obj object)` | ⚠ deprecated — use `treenode.prev` |
| `pt` | — | `(str text)` | ⚠ deprecated — use `the` |
| `query` | num | `(str query[, node/num/str p1, node/num/str p2, ...])` | ⚠ deprecated — use `Table.query` |
| `radianstodegrees` | num | `(num value)` | ⚠ deprecated — use `Math.degrees` |
| `rank` | node | `(obj parent, num index)` | ⚠ deprecated — use `treenode.subnodes` |
| `rankobj` | node | `(obj container, num rank)` | ⚠ deprecated — use `the` |
| `reftable` | — | `(str/num globaltable)` | ⚠ deprecated — use `the` |
| `relloc` | num | `(obj object, obj containerobj, num coordinate)` | ⚠ deprecated — use `Vec3.project` |
| `repeat` | — | `(num nr, expression)` | ⚠ deprecated |
| `reset` | — | `()` | ⚠ deprecated — use `resetmodel` |
| `resumeanimation` | — | `(obj object, num/str animation[, num resumeAtType, num resumeAtVal])` | ⚠ deprecated — use `the` |
| `resumeinput` | — | `(obj object)` | ⚠ deprecated — use `Object.input.resume` |
| `resumeobject` | — | `(obj object [, num id, num stateprofile ] )` | ⚠ deprecated — use `Object.resume` |
| `resumeoutput` | — | `(obj object)` | ⚠ deprecated — use `Object.output.resume` |
| `richeditaddtext` | — | `(obj richedit, str text, num protected (0/1))` | ⚠ deprecated |
| `richedittoken` | — | `(obj richedit, num tokenindex)` | ⚠ deprecated |
| `round` | num | `(num value[, num precision])` | ⚠ deprecated — use `Math.round` |
| `runexpertfit` | — | `()` | ⚠ deprecated |
| `savefpk` | — | `(node servicepackhead)` | ⚠ deprecated — use `user` |
| `senddelayedmessage` | — | `(obj toobject, num delaytime, obj fromobject [, num par1, num par2, num par3])` | ⚠ deprecated — use `Object.message` |
| `sendmessage` | — | `(obj toobject, obj fromobject [, var par1, var par2, var par3])` | ⚠ deprecated — use `Object.message` |
| `set` | node | `(node thenode, num value)` | ⚠ deprecated — use `treenode.value` |
| `setcenter` | node | `(obj object, num x, num y, num z)` | ⚠ deprecated — use `Object.setLocation` |
| `setcolor` | — | `(obj object, num red, num green, num blue)` | ⚠ deprecated |
| `setcurrent` | — | `(thing)` | ⚠ deprecated |
| `setitem` | — | `(thing)` | ⚠ deprecated |
| `setitemtype` | — | `(obj object, num value)` | ⚠ deprecated — use `labels` |
| `setlabel` | — | `(obj node, str/num label, num/str/node value)` | ⚠ deprecated — use `treenode.labelProperties` |
| `setlabelnum` | — | `(obj object, str/num label, num value)` | ⚠ deprecated — use `treenode.labelProperties` |
| `setlabelstr` | — | `(obj object, str/num label, str value)` | ⚠ deprecated — use `treenode.labelProperties` |
| `setloc` | node | `(obj object, num x, num y, num z[, num xFactor, num yFactor, num zFactor])` | ⚠ deprecated — use `Object.location` |
| `setname` | node | `(obj object, str value)` | ⚠ deprecated — use `treenode.name` |
| `setnodename` | — | `(obj/node object, str value)` | ⚠ deprecated — use `treenode.name` |
| `setnodenum` | setnodenum | `(node thenode, num value)` | ⚠ deprecated — use `treenode.value` |
| `setnodestr` | node | `(node thenode, str value)` | ⚠ deprecated — use `treenode.value` |
| `setobjectimageindex` | — | `(obj object, num index)` | ⚠ deprecated — use `the` |
| `setport` | — | `(thing)` | ⚠ deprecated |
| `setquerynode` | — | `(node queryNode)` | ⚠ deprecated — use `Table.query` |
| `setrank` | node | `(obj/node object, num index)` | ⚠ deprecated — use `treenode.rank` |
| `setrot` | — | `(obj object, num rx, num ry, num rz)` | ⚠ deprecated — use `Object.rotation` |
| `sets` | — | `(node thenode, str value)` | ⚠ deprecated — use `treenode.value` |
| `setsize` | — | `(obj object, num sx, num sy, num sz)` | ⚠ deprecated — use `Object.size` |
| `setstate` | — | `(obj object, num state [, num profilenr])` | ⚠ deprecated — use `Object.setState` |
| `setstate_s` | — | `(obj object, str statename)` | ⚠ deprecated — use `Object.stats.state` |
| `settable` | — | `(node table, num sx, num sy[, num type, num overwrite])` | ⚠ deprecated — use `the` |
| `settableheader` | — | `(string/num/node table, num row/col, num rowcolnr, string value)` | ⚠ deprecated — use `Table.setColHeader` |
| `settablenum` | — | `(str/node/num table, num row, num col, num value)` | ⚠ deprecated — use `the` |
| `settablesize` | — | `(str/num/node table, num rows, num cols [, num datatype, num overwrite y/n])` | ⚠ deprecated — use `Table.setSize` |
| `settablestr` | — | `(str/node/num table, num row, num col, str value)` | ⚠ deprecated — use `the` |
| `settrackedvariable` | — | `(str/node trackedvar, num value[, num rate])` | ⚠ deprecated — use `the` |
| `setundos` | — | `(num maxundos)` | ⚠ deprecated — use `this` |
| `showgraphicinfo` | — | `(num showhide, str filename)` | ⚠ deprecated |
| `sign` | num | `(num value)` | ⚠ deprecated — use `Math.sign` |
| `sin` | num | `(num angle)` | ⚠ deprecated — use `Math.sin` |
| `sorttable` | — | `(str/node/num table, num column)` | ⚠ deprecated — use `Table.sort` |
| `spaceglobal` | — | `()` | ⚠ deprecated — use `drawtomodelscale` |
| `spaceobject` | — | `()` | ⚠ deprecated — use `drawtoobjectscale` |
| `sqr` | num | `(num value)` | ⚠ deprecated — use `Math.sqr` |
| `sqrt` | num | `(num value)` | ⚠ deprecated — use `Math.sqrt` |
| `startanimation` | — | `(obj object, num/str animation[, num speedtype, num speedvalue, num repeattype, num repeatvalue])` | ⚠ deprecated — use `the` |
| `statisticaltime` | num | `()` | ⚠ deprecated — use `Model.statisticalTime` |
| `stopanimation` | — | `(obj object, num/str animation)` | ⚠ deprecated — use `the` |
| `stopinput` | — | `(obj object)` | ⚠ deprecated — use `Object.input.stop` |
| `stopobject` | — | `(obj object, num state [, num id, num priority, num stateprofile] )` | ⚠ deprecated — use `Object.stop` |
| `stopoutput` | — | `(obj object)` | ⚠ deprecated — use `Object.output.stop` |
| `store` | — | `()` | ⚠ deprecated — use `this` |
| `stringcopy` | str | `(str string, num start, num length)` | ⚠ deprecated — use `string.substr` |
| `stringlen` | num | `(str string)` | ⚠ deprecated — use `string.length` |
| `stringpart` | str | `(str string, num start, num length)` | ⚠ deprecated — use `string.substr` |
| `stringreplace` | str | `(str stringtosearch, str searchfor, str replacewith)` | ⚠ deprecated — use `string.replace` |
| `stringreturn` | — | `(str returnstring, ptr callpoint)` | ⚠ deprecated |
| `stringsearch` | num | `(str stringtosearch, str searchfor, int startindex[, int flags])` | ⚠ deprecated — use `string.search` |
| `stringtonum` | num | `(str string)` | ⚠ deprecated — use `string.toNum` |
| `stringtopchar` | str | `(str string)` | ⚠ deprecated — use `apchar` |
| `strquote` | str | `(str string)` | ⚠ deprecated |
| `sub` | num | `(num value1, num value2)` | ⚠ deprecated — use `the` |
| `swaptablecols` | — | `(str/num/node table, num column, num column2)` | ⚠ deprecated — use `Table.swapCols` |
| `swaptablerows` | — | `(str/num/node table, num row, num row2)` | ⚠ deprecated — use `Table.swapRows` |
| `switch_hidebase` | num | `(obj object[, num off/on])` | ⚠ deprecated |
| `tan` | num | `(num angle)` | ⚠ deprecated — use `Math.tan` |
| `time` | num | `()` | ⚠ deprecated — use `Model.time` |
| `trackedvariable` | — | `(str name)` | ⚠ deprecated — use `the` |
| `transfernode` | — | `(node movenode, node container)` | ⚠ deprecated — use `treenode.up` |
| `travelto` | — | `(obj object, obj spline, num x, num y, num z, num vmax, num acc, num dec)` | ⚠ deprecated — use `kinematics` |
| `traveltoupdate` | — | `(obj object [, obj spline])` | ⚠ deprecated — use `kinematics` |
| `trunc` | num | `(num value)` | ⚠ deprecated — use `Math.trunc` |
| `undos` | — | `(num off/on)` | ⚠ deprecated — use `this` |
| `up` | node | `(obj object)` | ⚠ deprecated — use `treenode.up` |
| `updateanimation` | — | `(obj object [, num updatetime, num animationnr])` | ⚠ deprecated — use `the` |
| `updatetoolsmenu` | — | `()` | ⚠ deprecated — use `the` |
| `userfunction_n` | — | `(obj object, num index [, num param_a,...])` | ⚠ deprecated — use `nodefunction` |
| `userfunction_s` | — | `(obj object, str name [, num param_a,...])` | ⚠ deprecated — use `nodefunction` |
| `var` | node | `(obj object, num index)` | ⚠ deprecated — use `getvarnode` |
| `var_s` | node | `(obj object, str name)` | ⚠ deprecated — use `getvarnode` |
| `vectorprojectx` | num | `(obj projectfrom, num fromx, num fromy, num fromz, obj projectonto)` | ⚠ deprecated — use `Vec3.project` |
| `vectorprojecty` | num | `(obj projectfrom, num fromx, num fromy, num fromz, obj projectonto)` | ⚠ deprecated — use `Vec3.project` |
| `vectorprojectz` | num | `(obj projectfrom, num fromx, num fromy, num fromz, obj projectonto)` | ⚠ deprecated — use `Vec3.project` |
| `xcenter` | num | `(obj object)` | ⚠ deprecated — use `Object.getLocation` |
| `xloc` | num | `(obj object)` | ⚠ deprecated — use `Object.location` |
| `xrot` | num | `(obj object)` | ⚠ deprecated — use `Object.rotation` |
| `xsize` | num | `(obj object)` | ⚠ deprecated — use `Object.size` |
| `ycenter` | num | `(obj object)` | ⚠ deprecated — use `Object.getLocation` |
| `yloc` | num | `(obj object)` | ⚠ deprecated — use `Object.location` |
| `yrot` | num | `(obj object)` | ⚠ deprecated — use `Object.rotation` |
| `ysize` | num | `(obj object)` | ⚠ deprecated — use `Object.size` |
| `zcenter` | num | `(obj object)` | ⚠ deprecated — use `Object.getLocation` |
| `zloc` | num | `(obj object)` | ⚠ deprecated — use `Object.location` |
| `zrot` | num | `(obj object)` | ⚠ deprecated — use `Object.rotation` |
| `zsize` | num | `(obj object)` | ⚠ deprecated — use `Object.size` |

